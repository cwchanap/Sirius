# HPA-376 Sirius UI Lifecycle Contract

**Implementation-start commit:** `33e9567b04d06bf532ce1d7deb3b6432cb5a927a`
**Runtime:** Godot 4.6.2, .NET 8.0, GdUnit4
**Baseline result:** Passed: 863, Failed: 0, Skipped: 0, Total: 863.
**Baseline orphan evidence:** 9 orphan-warning lines. Distinct messages: `Detected <7> orphan nodes during test execution!`; `Detected <10> orphan nodes during test execution!`; `Detected <1> orphan nodes during test execution!`.

## Contract policy

Each row records:

1. observed `file:member` evidence;
2. required migration behavior;
3. one disposition: `Preserve`, `Fix in HPA-376`, or `Replace in HPA-378/379`;
4. protecting test evidence or downstream owner.

Unknown or absent focus, cursor, and restoration behavior is stated explicitly rather than inferred from Godot defaults. “HUD retained” means the current gameplay HUD is not explicitly hidden by the cited owner; it does not imply a shared HUD policy.

## Flow lifecycle matrix

| ID | Observed evidence | Entry and parent | Pause and input block | HUD and cursor | Focus | Input surfaces and topmost receiver | Nested behavior | Terminal signal/count | Cleanup and restoration | Required migration contract | Disposition | Evidence/owner |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `MAIN-ROOT` | `scripts/ui/MainMenu.cs:_Ready` | Startup or return-to-title; no parent screen | Tree pause is not changed; Main Menu buttons own UI input | Main Menu visible; no explicit cursor policy | Scene-authored/default focus is not explicitly restored | Buttons receive explicit actions; no root Cancel handler is observed | Load, Settings, and messages layer above root | No terminal signal; Start changes scene and Quit exits | Root remains until scene change or application exit | Preserve Main Menu as the root; child close restores only Main Menu; root Cancel remains a no-op unless separately approved | `Preserve` | `MainMenuTest`; HPA-378/379 for common focus/cursor ownership |
| `MAIN-LOAD` | `scripts/ui/MainMenu.cs:_on_load_button_pressed`; `scripts/ui/MainMenu.cs:OnLoadDialogClosed` | Load button from Main Menu; Main Menu is parent | Parent remains present; `SaveLoadDialog` is exclusive; no tree-pause mutation | Main Menu remains beneath dialog; no explicit cursor policy | Load dialog focus is not explicitly assigned | Slot/button input goes to Save/Load; ordinary close returns to Main Menu | No-save and unavailable-save paths use `ShowMessage`; corrupt/load failure also becomes a message | `LoadSlotSelected` or `DialogClosed`; parent cleanup is intended once | `CleanupLoadDialog` disconnects and frees; successful load hands off through `PendingLoadData` | Preserve empty/populated/error routing, successful handoff, and restoration to Main Menu; terminal handling must remain idempotent | `Preserve` | `MainMenuTest`; `SaveLoadDialogTest` |
| `MAIN-SETTINGS` | `scripts/ui/MainMenu.cs:_on_settings_button_pressed`; `scripts/ui/MainMenu.cs:OnSettingsClosed` | Settings button from Main Menu; Main Menu is parent | Settings consumes input; inherits unpaused Main Menu state | Main Menu remains beneath full overlay; cursor not explicitly owned | Settings grabs its first control; Main Menu focus restoration is absent | Settings owns Apply, Cancel, and remapped Cancel | Dropdown/key capture remain Settings children | `Closed`; Main Menu currently cleans up on each received emission | `OnSettingsClosed` disconnects, frees, and returns to Main Menu | Preserve staged Apply/discard behavior and Main Menu restoration; controller emission count is owned by `SET-MAIN` | `Preserve` | `MainMenuTest`; `SettingsMenuControllerTest` |
| `MAIN-MESSAGE` | `scripts/ui/MainMenu.cs:ShowMessage` | Recoverable information/error over Main Menu | Exclusive `AcceptDialog`; no tree-pause mutation | Main Menu remains below; no explicit cursor policy | Default dialog focus only; not specified in source | Dialog Confirm/Cancel/window close is topmost | May be invoked instead of Load/Settings | No parent signal; popup also queues free after two seconds | Popup frees; Main Menu remains usable | Preserve recoverable topmost message behavior and Main Menu restoration; repeated cleanup must be harmless | `Preserve` | `MainMenuTest` |
| `MAIN-QUIT` | `scripts/ui/MainMenu.cs:_on_quit_button_pressed` | Explicit Quit from root; no active gameplay progress | No pause change; quits immediately | Main Menu remains until application exit; cursor unspecified | Quit button is the invoking control | Explicit Quit only; root Cancel does not confirm | None | `SceneTree.Quit`; one explicit action | Application exits; no gameplay cleanup is required | Preserve immediate root Quit without quit-with-risk confirmation | `Preserve` | `MainMenuTest` |
| `EXP-GAMEPLAY` | `scripts/game/Game.cs:HandlePauseMenuInput` | Active gameplay; root gameplay context | World continues; gameplay owns input when no blocker; `pause_menu` opens Pause | Gameplay HUD retained; cursor policy absent | No UI focus; gameplay owns input | `pause_menu` falls through to gameplay and opens Pause | Blocking flows pre-empt gameplay in documented priority | No terminal signal | Pause becomes the active child; closing it targets gameplay | Preserve gameplay fallback and exactly one topmost receiver; host later owns shared pause/cursor policy | `Preserve` | `GameInputLifecycleTest`; `GameTest.PauseMenu_WhenNoPauseMenu_OpensPauseMenuDialog` |
| `EXP-PROMPT` | `scripts/game/Game.cs:UpdateInteractionPrompt` | Adjacent treasure, switch, or riddle while exploring | Hidden while battle, NPC, or world-interaction flags are active | Prompt is part of gameplay HUD; cursor unaffected by source | No focus | No input ownership; describes the current `interact` target | Must track each blocking child and floor replacement | No signal | Recomputed on movement/facing and selected world paths; battle/NPC boundaries are incomplete | Hide during every blocking flow and refresh when battle/NPC/world lifecycles end, without stealing focus or input | `Fix in HPA-376` | `GameInputLifecycleTest`; existing treasure/puzzle `GameTest` methods |
| `EXP-FLOOR-TRANSITION` | `scripts/game/Game.cs:OnFloorLoaded`; `scripts/game/Game.cs:OnFloorChanged` | Stair transition within gameplay; `FloorManager` replaces the floor | No modal pause; live gameplay references are rebound | Gameplay HUD retained; prompt refresh is deferred; cursor unchanged by source | No UI focus | Floor transition owns world replacement; gameplay resumes afterward | Pending save-spawn validation can surface a corrupt-save error | `FloorLoaded` and `FloorChanged` subscriptions; no UI terminal signal | Old grid signals/display are cleaned; prompt and camera setup are deferred on new floor | Preserve live HUD, signal rebinding, and deferred prompt restoration after the new floor is ready | `Preserve` | `GameInputLifecycleTest`; `GameTest` floor-transition coverage |
| `INV-GAMEPLAY` | `scripts/game/Game.cs:_Input`; `scripts/ui/InventoryMenuController.cs:OpenMenu`; `scripts/ui/InventoryMenuController.cs:CloseMenu` | `toggle_inventory` from exploration; gameplay is parent | Open sets `SceneTree.Paused = true`; close currently forces false | Inventory overlays gameplay; gameplay HUD is not explicitly hidden; cursor/focus ownership absent | No initial focus is explicitly assigned | Inventory receives `ui_cancel` and toggle; `Game` closes visible Inventory before Pause | No child modal is documented | No terminal signal; visibility and pause restoration are observable | Close hides and currently overwrites incoming pause state | Snapshot the incoming pause value once per open cycle, pause while open, restore exactly that value on close/free, and make repeated close harmless | `Fix in HPA-376` | `InventoryMenuControllerTest`; `GameInputLifecycleTest` |
| `INV-BLOCKED` | `scripts/game/Game.cs:_Input` | Inventory request while Settings, Save/Load, Pause, battle, NPC, or world interaction is active | Request is rejected by controller/reference and interaction-flag guards | Active parent HUD/surface remains unchanged | Existing topmost focus remains | Active blocking layer receives input; Inventory does not open | Active child remains intact | No Inventory terminal signal | Nothing to clean or restore | Preserve rejected entry for all current blockers; one blocked request must not mutate visibility or pause state | `Preserve` | existing blocked-entry `GameTest` methods; `GameInputLifecycleTest` |
| `INV-PAUSE` | `scripts/game/Game.cs:_Input`; `scripts/ui/InventoryMenuController.cs:OpenMenu` | Approved future entry from Pause; current graph blocks it | Current Pause does not pause and blocks Inventory; no reachable lifecycle exists | Current HUD/cursor ownership absent | No focus contract exists in current controller | Current Pause remains receiver; Inventory cannot become topmost | No current nesting | No terminal signal | No current restoration path | Host-owned Inventory child of Pause: world paused, gameplay HUD hidden, Pause inert, cursor visible, last valid tab/slot or first equipment slot focused, close returns to Pause while preserving domain state | `Replace in HPA-378/379` | HPA-378/379 |
| `PAUSE-ROOT` | `scripts/game/Game.cs:ShowPauseMenu`; `scripts/game/Game.cs:CleanupPauseMenu`; `scripts/ui/PauseMenuDialog.cs:OnCloseRequested` | `pause_menu` from exploration; gameplay is parent | Current dialog does not set `SceneTree.Paused`; it blocks only through modal/input routing | Gameplay HUD remains visible beneath dialog; no explicit cursor policy | Resume receives deferred focus | `pause_menu`, `ui_cancel`, window close, and Resume target Pause | Settings and Save/Load are hidden-parent children | `ResumeRequested`; guarded once per visible cycle for close paths | `CleanupPauseMenu` disconnects/frees and returns to gameplay | Host-owned Pause must pause world, keep gameplay HUD visible but inert under scrim, show cursor, focus Resume, and restore gameplay once; preserve child/domain cleanup | `Replace in HPA-378/379` | `PauseMenuDialogTest`; `GameTest`; HPA-378/379 |
| `PAUSE-SETTINGS` | `scripts/game/Game.cs:OnPauseSettingsRequested`; `scripts/game/Game.cs:OnPauseSettingsClosed` | Settings from Pause; hidden Pause is parent | Inherits current Pause state; parent is hidden and inert | Settings visible; gameplay HUD and cursor have no explicit owner | Settings first control; restored Pause focuses Resume when shown | Settings owns Cancel; Pause must not receive it | Dropdown and capture remain above Settings | Settings `Closed`; Pause restoration is deferred | Settings frees; hidden Pause is preserved and then re-popuped | Preserve child-only close and deferred restoration to Pause; root Pause pause/cursor policy remains downstream | `Preserve` | `GameTest`; `SettingsMenuControllerTest`; `GameInputLifecycleTest` |
| `PAUSE-SAVELOAD` | `scripts/game/Game.cs:OnPauseSaveRequested`; `scripts/game/Game.cs:OnPauseLoadRequested`; `scripts/game/Game.cs:CleanupSaveDialogAndRestorePause` | Save/Load from Pause; hidden Pause is parent | Inherits current Pause state; Save/Load is exclusive | Save/Load visible; Pause hidden; cursor not explicitly owned | Slot focus is not explicitly assigned | Child confirmation first, then Save/Load Cancel/window close | Overwrite and errors may be topmost | Save/load operation signal or `DialogClosed`; controller count is owned by `SAVE-PAUSE` | Save dialog frees and hidden Pause is re-popuped on close/failure | Preserve invoking-Pause snapshot, child-first routing, operation cleanup, and restoration to the same Pause | `Preserve` | `GameTest`; `SaveLoadDialogTest`; `GameInputLifecycleTest` |
| `PAUSE-QUIT-TO-MAIN` | `scripts/game/Game.cs:OnPauseQuitRequested` | Quit to Main from Pause | Current path immediately removes Pause and changes scene | Pause/gameplay disappear on transition; cursor unspecified | Quit button invokes | Explicit Quit acts immediately; no confirmation | Required future confirmation is absent | `QuitToMenuRequested`; one button action | Current Pause cleanup occurs before scene transition | Insert explicit quit-with-risk child confirmation; generic Cancel must return to Pause; explicit safe confirm navigates; preserve current Pause and gameplay-domain cleanup | `Replace in HPA-378/379` | HPA-378/379; `PauseMenuDialogTest` for current signal |
| `PAUSE-RESTORE-PENDING` | `scripts/game/Game.cs:OnPauseSettingsClosed`; `scripts/game/Game.cs:RestorePauseMenuAfterSettings`; `scripts/game/Game.cs:HandlePauseMenuInput` | Settings has closed; hidden Pause is pending deferred restoration | Re-entrant `pause_menu` is consumed as a no-op | Visible stack is not changed; cursor policy absent | Pause focus is restored when popup reopens | Pending guard is topmost for `pause_menu` | No child can be reopened during the guard | No signal | Deferred call clears guard and shows existing Pause | Preserve the atomic outcome: re-entrant back input changes no layer and restored Pause remains the parent; boolean mechanism may be replaced | `Preserve` | `GameInputLifecycleTest`; `GameTest.PauseMenu_WhenSettingsCloseTriggersEscape_DefersPauseMenuRestore` |
| `SET-MAIN` | `scripts/ui/SettingsMenuController.cs:OpenSettings`; `scripts/ui/SettingsMenuController.cs:OnApplyPressed`; `scripts/ui/SettingsMenuController.cs:OnCancelPressed` | Settings under Main Menu | Inherits unpaused parent; Settings consumes non-navigation input | Full overlay shown; parent inert; cursor not explicitly owned | Master-volume slider receives focus | Apply/Cancel/remapped Cancel own terminal input | Dropdown and key capture pre-empt root Settings | `Closed`; currently unguarded across competing/repeated terminal paths | Main Menu frees controller on first signal | Emit `Closed` exactly once per open cycle for Apply or discard; restore only Main Menu and retain staged-edit semantics | `Fix in HPA-376` | `SettingsMenuControllerTest`; `MainMenuTest` |
| `SET-PAUSE` | `scripts/ui/SettingsMenuController.cs:OpenSettings`; `scripts/game/Game.cs:OnPauseSettingsClosed` | Settings under hidden Pause | Inherits Pause state; Settings consumes non-navigation input | Parent remains hidden/inert; cursor not explicitly owned | Master-volume slider receives focus; Pause later focuses Resume | Apply/Cancel/remapped Cancel own terminal input | Dropdown and capture pre-empt root Settings | `Closed`; currently unguarded across competing/repeated terminal paths | Game frees Settings and defers restoration of existing Pause | Emit `Closed` exactly once per open cycle and restore only invoking Pause with its inherited pause state | `Fix in HPA-376` | `SettingsMenuControllerTest`; `GameInputLifecycleTest` |
| `SET-DROPDOWN` | `scripts/ui/SettingsMenuController.cs:IsPopupOpen`; `scripts/ui/SettingsMenuController.cs:_Input` | Resolution/difficulty `OptionButton` under Settings in either parent | Inherits Settings state; popup is topmost | Settings remains visible; cursor unchanged by source | Popup owns temporary focus by Godot; no extra fallback inferred | `ui_cancel`/Pause reaches the open popup before Settings | Closing popup leaves Settings active and staged edits intact | No Settings terminal signal | Popup closes; Settings remains | Preserve child-first Cancel, no Settings close, no gameplay/Pause fallthrough, and return to Settings controls | `Preserve` | `SettingsMenuControllerTest.EscapeKey_WhenResolutionPopupOpen_DoesNotCloseSettings`; difficulty equivalent |
| `SET-CAPTURE` | `scripts/ui/SettingsMenuController.cs:_Input`; `scripts/ui/SettingsMenuController.cs:CancelKeyCapture` | Ordinary key capture inside Settings | Settings remains active and blocks gameplay input | Settings HUD state unchanged; cursor unchanged by source | Invoking binding button remains the logical target | Capture receives candidate, cancel, duplicate, and reserved bindings before Settings | Error label is local feedback, not a modal | No `Closed`; capture ends once on accept/cancel | Button text/error reset; Settings remains active | Preserve valid accept, duplicate/reserved rejection, edited-Pause-key Cancel, and return to the same Settings parent | `Preserve` | `SettingsMenuControllerTest` capture cases |
| `SET-CAPTURE-PAUSE` | `scripts/ui/SettingsMenuController.cs:ShouldCancelOrClose`; `scripts/ui/SettingsMenuController.cs:IsReservedKey` | Capture of `pause_menu` inside Settings | Settings stays active; Escape/Pause is data, not generic close | Settings remains visible; cursor unchanged by source | Pause binding button is capture target | Capture is topmost; Escape is accepted as candidate | Error feedback remains local | No `Closed`; accepted candidate ends capture once | Binding text updates; Settings remains | Preserve Pause-binding exception across remapped/unbound keyboard and controller paths; do not close Settings or open Pause | `Preserve` | `SettingsMenuControllerTest.InputDuringCapture_PauseMenuEscape_AcceptsBinding`; joypad equivalent |
| `SAVE-MAIN` | `scripts/ui/SaveLoadDialog.cs:ShowDialog`; `scripts/ui/SaveLoadDialog.cs:OnCancelPressed`; `scripts/ui/SaveLoadDialog.cs:OnCloseRequested` | Save/Load under Main Menu; Main Menu is parent | Inherits unpaused state; exclusive dialog blocks parent | Main Menu retained beneath dialog; cursor not explicitly owned | Slot focus is not explicitly assigned | Slot buttons, Cancel, `ui_cancel`, and window close target Save/Load after any child | Overwrite confirmation and errors are higher priority | `DialogClosed`, load/save selection, or Main Menu request; close is currently unguarded | Caller disconnects/frees and restores Main Menu or transitions | Add `ui_cancel` handling and exactly one mutually exclusive terminal emission per show cycle; child Cancel must not close parent; restore Main Menu | `Fix in HPA-376` | `SaveLoadDialogTest`; `MainMenuTest` |
| `SAVE-PAUSE` | `scripts/ui/SaveLoadDialog.cs:ShowDialog`; `scripts/game/Game.cs:CleanupSaveDialogAndRestorePause` | Save/Load under hidden Pause | Inherits current Pause state; exclusive dialog blocks parent | Save/Load visible; Pause hidden; cursor not explicitly owned | Slot focus is not explicitly assigned | Child first, then `ui_cancel`, Cancel button, or window close targets Save/Load | Overwrite/error remains topmost | Exactly one of operation, `DialogClosed`, or Main Menu request is required; currently unguarded | Caller frees dialog and restores the same Pause on close/failure | Add `ui_cancel` handling and exactly-once mutually exclusive terminal emission; preserve hidden-Pause cleanup/restoration and operation semantics | `Fix in HPA-376` | `SaveLoadDialogTest`; `GameTest`; `GameInputLifecycleTest` |
| `SAVE-OVERWRITE` | `scripts/ui/SaveLoadDialog.cs:ShowOverwriteConfirmation`; `scripts/ui/SaveLoadDialog.cs:DismissActiveChildDialog` | Existing populated save slot; Save dialog is parent | Inherits parent pause; parent remains inert | Parent Save screen remains; cursor not explicitly owned | Confirmation default focus is not specified in source | Confirmation Confirm/Cancel/window close is topmost; `Game` dismisses child first | One child only; stale child callbacks are ignored | Confirm emits one `SaveSlotSelected`; cancel emits no parent terminal | Child frees and clears pending slot; parent remains open on cancel | Preserve child-only cancel/window close, explicit overwrite, stale-child rejection, and return to the invoking Save screen | `Preserve` | `SaveLoadDialogTest`; `GameTest.PauseMenu_WhenSaveDialogHasChildDialog_EscDismissesChildOnly` |
| `SAVE-CORRUPT` | `scripts/ui/SaveLoadDialog.cs:RefreshSlotInfo`; `scripts/game/Game.cs:ShowCorruptedSaveError` | Corrupt/unavailable slot under Main Menu or Pause/gameplay | Corrupt slot is disabled in Load; blocking error owns input when surfaced | Invoking parent remains or gameplay error overlays; cursor not explicitly owned | Error default focus only; not inferred | Topmost error receives Cancel/Confirm | Save dialog may already be cleaned/restored before error | Error dismissal only; no save/load terminal duplication | Error frees; already-restored parent remains | Preserve disabled corrupt-load behavior, one topmost recoverable error, and restoration to the invoking parent without Pause toggling | `Preserve` | `SaveLoadDialogTest`; `GameInputLifecycleTest`; existing `GameTest` corrupt-save cases |
| `SAVE-ERROR` | `scripts/game/Game.cs:ShowSaveError`; `scripts/game/Game.cs:CleanupSaveDialogAndRestorePause` | Ordinary operation/open error under Main Menu, Pause, or gameplay | Save flow is cleaned before error where applicable; error is topmost | Restored parent remains below; cursor not explicitly owned | Error default focus only; not inferred | Error Confirm/Cancel receives first; `pause_menu` also dismisses active error | Previous active error is replaced | Error dismissal has no parent terminal signal | Popup frees and leaves already-restored parent unchanged | Preserve topmost error routing and exactly one cleanup; dismiss only error and never toggle/open/close Pause as a side effect | `Preserve` | `GameInputLifecycleTest`; `GameTest` save/load failure cases |
| `SAVE-QUIT-TO-MAIN` | `scripts/ui/SaveLoadDialog.cs:OnMainMenuPressed`; `scripts/game/Game.cs:OnMainMenuRequested` | Main Menu button in in-game Save flow; Save/Load and possibly hidden Pause are parents | Current path hides dialog and navigates immediately | Current layers disappear on transition; cursor unspecified | Main Menu button invokes | Explicit action acts immediately; no quit-with-risk child exists | Hidden Pause may also need cleanup | `MainMenuRequested` only; deliberately not `DialogClosed` | Game cleans Save/Load and Pause before scene transition | Insert explicit quit-with-risk child; generic Cancel returns to Save/Load; explicit safe confirm navigates; preserve Save/Load, hidden Pause, and gameplay-domain cleanup | `Replace in HPA-378/379` | HPA-378/379; `SaveLoadDialogTest` for current exclusivity |
| `BATTLE-PREP` | `scripts/ui/BattleManager.cs:StartBattle`; `scripts/ui/BattleManager.cs:OnCloseRequested` | Encounter opens Battle before automatic turns; gameplay is parent | `IsInBattle` blocks gameplay; timer not running until Start | Battle presentation visible; existing HUD policy; cursor not explicitly set | Start button is actionable; explicit focus is not established in source | Start and existing escape/window-close path own input | Optional pre-battle item panel | Escape emits `BattleFinished(false, true)` once via `_resultEmitted` | Dialog hides/frees; Game ends battle and restores gameplay | Preserve preparation, optional item selection, existing escape policy, once-only result, and cleanup before gameplay restoration | `Preserve` | `BattleManagerTest`; `GameInputLifecycleTest` |
| `BATTLE-AUTO` | `scripts/ui/BattleManager.cs:StartBattle`; `scripts/ui/BattleManager.cs:OnCloseRequested` | Start from preparation; Battle is parent flow | Battle timer runs; `IsInBattle` blocks gameplay | Battle presentation retained; cursor follows existing combat policy | No gameplay focus; battle controls own actions | Timer/controller and permitted escape path own input | Battle-local effects and pending loot are owned children/state | Victory/defeat/escape result once via `_resultEmitted` | Timer stops and combatant transient effects clear before result | Preserve automatic combat progression, escape policy, timer/effect cleanup, and exactly-one result | `Preserve` | `BattleManagerTest`; `GameInputLifecycleTest` |
| `BATTLE-ESC-PREP` | `scripts/ui/BattleManager.cs:OnCloseRequested`; `scripts/game/Game.cs:HandlePauseMenuInput` | Escape before Start; Battle preparation is parent | Gameplay remains blocked until escape result cleanup | Battle closes; gameplay HUD remains underneath; cursor unspecified | Battle dialog is receiver | `pause_menu`, `ui_cancel`, or window close routes to battle escape | Optional item panel is discarded | One `BattleFinished(false, true)` | Dialog frees; Game clears battle state; enemy remains | Preserve pre-start escape payload, no reward/enemy removal, no Pause behind Battle, and exactly-once cleanup | `Preserve` | `BattleManagerTest`; `GameInputLifecycleTest` |
| `BATTLE-ESC-ACTIVE` | `scripts/ui/BattleManager.cs:OnCloseRequested`; `scripts/ui/BattleManager.cs:EndBattleWithEscape` | Escape during automatic combat | Timer stops; battle/gameplay block clears after result | Escape result is shown; gameplay HUD beneath; cursor unspecified | Continue becomes actionable; explicit focus absent | Battle escape is topmost; Pause must not open | Active effects and timer are battle-owned | One `BattleFinished(false, true)` | Timer/effects clean; enemy retained; presentation awaits dismissal | Preserve escape payload, timer/effect cleanup, enemy retention, and exactly-one result | `Preserve` | `BattleManagerTest`; `GameInputLifecycleTest` |
| `BATTLE-RESULT-VICTORY` | `scripts/ui/BattleManager.cs:EndBattle`; `scripts/game/Game.cs:OnBattleFinished`; `scripts/game/Game.cs:HandlePauseMenuInput` | Victory Continue/loot surface after domain result; Battle is presentation parent | `IsInBattle` is currently cleared while result remains visible | Battle result/loot remains; gameplay behind it; cursor/focus not explicitly owned | Continue is actionable but explicit focus is absent | Visible Battle result must receive Cancel/Continue before gameplay Pause fallback | Loot label is embedded/deferred within Battle | `BattleFinished(true, false)` already emitted; dismissal must not emit another result | Continue/Cancel frees Battle; gameplay resumes and enemy is removed | Route visible result through `ForceCloseAsEscape`, mark input handled, keep legacy Cancel dismissal, and never open Pause or duplicate the result | `Fix in HPA-376` | `BattleManagerTest`; `GameInputLifecycleTest.BattleResultCancelIsHandledAndClosesResultWithoutOpeningPause` |
| `BATTLE-RESULT-DEFEAT` | `scripts/ui/BattleManager.cs:EndBattle`; `scripts/game/Game.cs:OnBattleFinished` | Defeat Continue surface with delayed title navigation | `IsInBattle` clears; current unretained two-second timer can outlive owner | Defeat result stays visible; cursor/focus not explicitly owned | Continue actionable; explicit focus absent | Visible result owns Cancel/Continue before Pause | Delayed return-to-main callback is lifecycle state | One `BattleFinished(false, false)`; schedule at most one navigation | Battle cleanup must invalidate stale delayed navigation | Route visible result topmost and retain/guard the two-second navigation so cleanup/replacement prevents stale scene change without changing delay | `Fix in HPA-376` | `BattleManagerTest`; `GameInputLifecycleTest.DefeatReturnTimerIsOwnedAndDoesNotNavigateAfterCleanup` |
| `BATTLE-RESULT-ESCAPE` | `scripts/ui/BattleManager.cs:EndBattleWithEscape`; `scripts/game/Game.cs:OnBattleFinished`; `scripts/game/Game.cs:HandlePauseMenuInput` | Escape Continue surface after domain result | `IsInBattle` is cleared while result remains visible | Battle escape result remains; gameplay behind; cursor/focus not explicitly owned | Continue actionable; explicit focus absent | Visible Battle result receives Cancel/Continue before Pause | No reward child | `BattleFinished(false, true)` already emitted; dismissal must not emit another | Presentation frees; enemy and gameplay state are retained | Route visible result topmost, preserve legacy Cancel dismissal, mark input handled, and prevent Pause or duplicate result emission | `Fix in HPA-376` | `BattleManagerTest`; `GameInputLifecycleTest.BattleResultCancelIsHandledAndClosesResultWithoutOpeningPause` |
| `BATTLE-CLEANUP` | `scripts/ui/BattleManager.cs:OnCloseRequested`; `scripts/game/Game.cs:OnBattleDialogConfirmed`; `scripts/game/Game.cs:_ExitTree` | Any Battle terminal path | Timer and effects are stopped by result/escape; owner teardown disconnects signals | Battle presentation removed; gameplay/title becomes active | Focus fallback is not explicit | Competing close triggers converge on guarded result/cleanup | Pending loot and delayed callbacks must not survive owner | Result count guarded by `_resultEmitted`; cleanup may be requested repeatedly | Signals disconnect and dialog queues free; Game state is safety-reset if needed | Preserve once-only result, idempotent free/disconnect, timer/effect cleanup, and correct gameplay/title restoration | `Preserve` | `BattleManagerTest`; `GameInputLifecycleTest` |
| `NPC-DIALOGUE` | `scripts/ui/NpcInteractionController.cs:Begin`; `scripts/ui/DialogueDialog.cs:OnCloseRequested` | NPC interaction opens Dialogue; gameplay is parent | `IsInNpcInteraction` blocks gameplay; `Game` lets dialog receive Cancel | Dialogue modal visible; interaction prompt hidden; cursor not explicitly owned | Choice focus is not explicitly assigned | Choice/Continue, `ui_cancel`, and window close target Dialogue | Dialogue may transition to Shop or Heal | `DialogueOutcome` or `DialogueClosed`; currently unguarded and not mutually exclusive | Controller frees Dialogue and transitions or finishes | Emit exactly one mutually exclusive terminal outcome per open cycle; Cancel closes only Dialogue and cleanup restores gameplay or selected NPC child | `Fix in HPA-376` | `DialogueDialogTest`; `NpcInteractionControllerTest`; `GameInputLifecycleTest` |
| `NPC-TO-SHOP` | `scripts/ui/NpcInteractionController.cs:OnDialogueOutcome`; `scripts/ui/NpcInteractionController.cs:OpenShop` | `OpenShop` outcome from Dialogue; NPC interaction remains parent | Gameplay block remains continuously active | Dialogue frees before Shop popup; prompt stays hidden; cursor unspecified | Shop initial focus is not explicitly assigned | Transition consumes dialogue outcome; Shop becomes receiver | Missing shop data finishes safely | No interaction terminal during successful transition | Dialogue signals disconnect/free; Shop is created under same UI parent | Preserve atomic Dialogue-to-Shop replacement, continuous NPC block, safe missing-data cleanup, and later return target | `Preserve` | `NpcInteractionControllerTest`; `DialogueDialogTest`; `ShopDialogTest` |
| `NPC-SHOP` | `scripts/ui/ShopDialog.cs:OpenShop`; `scripts/ui/ShopDialog.cs:OnCloseRequested` | Shop under NPC interaction | `IsInNpcInteraction` blocks gameplay; exclusive dialog owns input | Gameplay HUD hidden by required target but not explicitly by current owner; cursor unspecified | Buy/Sell focus is not explicitly assigned | Buy/Sell, Close, Cancel, and window close target Shop | Feedback timer is local transient state | `ShopClosed` guarded once by `_closed` | Timer disconnected on free; controller frees Shop and finishes | Preserve buy/sell domain behavior, once-only close, feedback-timer cleanup, and restoration to invoking NPC/gameplay target | `Preserve` | `ShopDialogTest`; `NpcInteractionControllerTest`; `GameInputLifecycleTest` |
| `NPC-TO-HEAL` | `scripts/ui/NpcInteractionController.cs:OnDialogueOutcome`; `scripts/ui/NpcInteractionController.cs:OpenHeal` | `Heal` outcome from Dialogue; NPC interaction remains parent | Gameplay block remains continuously active | Dialogue frees before Heal popup; prompt stays hidden; cursor unspecified | Heal initial focus is not explicitly assigned | Transition consumes dialogue outcome; Heal becomes receiver | None beyond local feedback | No interaction terminal during transition | Dialogue frees; Heal is created under same UI parent | Preserve atomic Dialogue-to-Heal replacement, continuous NPC block, and restoration through the same interaction owner | `Preserve` | `NpcInteractionControllerTest`; `DialogueDialogTest`; `HealDialogTest` |
| `NPC-HEAL` | `scripts/ui/HealDialog.cs:OnHealPressed`; `scripts/ui/HealDialog.cs:OnCancelPressed` | Heal under NPC interaction | `IsInNpcInteraction` blocks gameplay; exclusive dialog owns input | Heal modal visible; prompt hidden; cursor unspecified | Heal/No Thanks focus is not explicitly assigned | Heal, No Thanks, Cancel, and window close target Heal | Insufficient gold/full HP is local feedback | `HealComplete` or `HealCancelled`; currently unguarded and not mutually exclusive | Controller frees Heal and finishes | Emit exactly one mutually exclusive terminal result per open cycle; preserve heal/gold rules; restore through NPC cleanup only once | `Fix in HPA-376` | `HealDialogTest`; `NpcInteractionControllerTest`; `GameInputLifecycleTest` |
| `NPC-CLEANUP` | `scripts/ui/NpcInteractionController.cs:Finish`; `scripts/game/Game.cs:OnNpcInteractionComplete` | Any Dialogue/Shop/Heal exit or open failure | `IsInNpcInteraction` remains until orchestration completes | Active dialog removed; prompt/gameplay HUD must refresh; cursor ownership absent | Gameplay focus fallback is not explicit | Once children are gone, gameplay regains input | All three possible dialogs are cleaned | `InteractionComplete` guarded once by `_finished` | Signals disconnect, dialogs free, and Game ends NPC interaction | Preserve idempotent orchestration cleanup and exactly one flag clear across every success, Cancel, and failure path | `Preserve` | `NpcInteractionControllerTest`; `GameInputLifecycleTest` |
| `WORLD-TREASURE` | `scripts/game/Game.cs:OnTreasureBoxOpenRequested` | Adjacent treasure from exploration | `IsInWorldInteraction` blocks Inventory/Pause during asynchronous opening | Prompt hidden; no standalone reward modal; cursor unchanged by source | No UI focus | Atomic world interaction consumes competing input | No presentation child | No UI terminal signal | `finally` clears world flag and refreshes prompt; grant, opened ID, and cell clear occur once | Preserve grant, opened-state persistence, cell clear, flag cleanup, and silent domain semantics; later UI consumes an already-resolved payload and never grants/reconstructs reward | `Preserve` | existing treasure `GameTest` methods |
| `WORLD-SWITCH` | `scripts/game/Game.cs:HandlePuzzleSwitch` | Adjacent puzzle switch from exploration | `IsInWorldInteraction` wraps atomic activation and blocks competing input | No modal; prompt refreshes; cursor unchanged | No focus | Atomic interaction is topmost; Pause is consumed by world block | May arm another puzzle phase but has no child UI | No UI terminal signal | `finally` clears flag and refreshes prompt | Preserve atomic activation result, no modal, exactly-once flag cleanup, and gameplay restoration | `Preserve` | existing puzzle `GameTest` methods; `GameInputLifecycleTest` |
| `WORLD-RIDDLE` | `scripts/game/Game.cs:OpenPuzzleRiddle`; `scripts/ui/PuzzleRiddleDialog.cs:OnCloseRequested`; `scripts/game/Game.cs:CleanupPuzzleRiddleDialog` | Riddle modal from exploration; gameplay is parent | `IsInWorldInteraction` blocks gameplay; `Game` lets dialog receive Cancel | Prompt hidden; modal visible; cursor not explicitly owned | Choice focus is not explicitly assigned | Choice, `ui_cancel`, and window close target Riddle | Dormant result can re-render same dialog with message | `ChoiceSelected` or `PuzzleRiddleClosed`; close currently unguarded | Game disconnects/frees, clears active riddle/flag, and refreshes prompt | Emit exactly one mutually exclusive terminal action per presentation cycle; retain retry behavior; cleanup and flag restoration occur once | `Fix in HPA-376` | `PuzzleRiddleDialogTest`; existing puzzle `GameTest` methods; `GameInputLifecycleTest` |
| `WORLD-CLEANUP` | `scripts/game/Game.cs:CleanupPuzzleRiddleDialog`; `scripts/game/Game.cs:OnTreasureBoxOpenRequested`; `scripts/game/Game.cs:HandlePuzzleSwitch` | Any treasure, switch, riddle, or failure exit | World flag is the gameplay-input block | Prompt is restored after cleanup; cursor/focus ownership absent | Gameplay focus fallback is not explicit | Gameplay becomes receiver only after active world owner ends | Riddle node/signals and atomic async state must be released | No shared UI terminal signal | Each path clears `IsInWorldInteraction`; riddle cleanup is repeat-safe at Game layer | Preserve exactly-once domain completion, idempotent node cleanup, and prompt/gameplay restoration on success, Cancel, and failure | `Preserve` | existing world `GameTest` methods; `PuzzleRiddleDialogTest`; `GameInputLifecycleTest` |
| `REWARD-TOAST` | `scripts/game/Game.cs:OnTreasureBoxOpenRequested`; no standalone reward surface exists | Future brief single reward from gameplay producer | Current treasure grant is atomic; no toast or pause lifecycle exists | Current grant is silent; target retains gameplay HUD and cursor | No focus | Current flow owns no Cancel surface | Future queue is absent | No current UI terminal signal | Current domain cleanup ends world interaction; no presentation cleanup exists | Queued non-blocking toast: no tree pause, HUD retained, cursor unchanged, no focus or Cancel owner, deterministic queue/lifetime cleanup; producer retains all grant/save authority | `Replace in HPA-378/379` | HPA-378/379; downstream reward screen |
| `REWARD-BLOCKING` | `scripts/ui/BattleManager.cs:EndBattle`; `scripts/ui/BattleManager.cs:ShowPendingLootDisplay`; no shared constellation exists | Future important/multiple/result acknowledgement from producer | Current loot is embedded in Battle; no shared blocking lifecycle exists | Target hides competing HUD, keeps parent inert, and shows cursor | Continue focused when actionable | Required acknowledgement ignores generic Cancel; explicit Continue owns exit | Producer-provided reward nodes/payload; shared child model absent | No shared terminal signal | Current Battle owns grant and cleanup; future presentation restores producer continuation | Blocking constellation pauses/inerts parent as required, uses visible cursor and Continue focus, cannot be Cancel-dismissed when required, restores producer target, and never grants or reconstructs reward | `Replace in HPA-378/379` | HPA-378/379; HPA-393 |
| `CONFIRM-ORDINARY` | `scripts/ui/SaveLoadDialog.cs:ShowOverwriteConfirmation`; `scripts/game/Game.cs:ShowSaveError` | Recoverable child over an invoking parent | Inherits parent pause/input block; topmost child owns input | Parent remains inert beneath child; cursor not explicitly owned | Default safe dialog action; exact focus not explicit | Child Confirm/Cancel/window close is topmost | Overwrite and recoverable messages represent current cases | Confirm performs safe action or Cancel returns; parent terminal is not duplicated | Child frees and restores only invoking parent | Preserve topmost child-only dismissal, explicit safe action, no parent fallthrough, and idempotent cleanup | `Preserve` | `SaveLoadDialogTest`; `GameInputLifecycleTest` |
| `CONFIRM-DESTRUCTIVE` | `scripts/game/Game.cs:OnPauseQuitRequested`; `scripts/game/Game.cs:OnMainMenuRequested`; no in-game quit confirmation exists | Future child over Pause or in-game Save/Load before title navigation | Current paths navigate immediately; target keeps invoking parent inert/paused | Parent remains beneath confirmation; cursor visible | Safe non-destructive action focused by default | Generic Cancel returns to parent; only explicit destructive confirm navigates | One topmost confirmation; no duplicate navigation | Exactly one cancel or confirm result | Confirm preserves existing Pause/Save/domain cleanup before navigation; Cancel restores invoking parent | Add explicit quit-with-risk confirmation for active progress, visible cursor, safe focus, no generic-Cancel confirmation, and current cleanup semantics | `Replace in HPA-378/379` | HPA-378/379 |
| `ERROR-TOPMOST` | `scripts/game/Game.cs:ShowSaveError`; `scripts/game/Game.cs:HandlePauseMenuInput` | Recoverable error over restored Pause/Main Menu or gameplay | Error pre-empts Pause/input fallback; does not change inherited pause state | Parent remains below; cursor not explicitly owned | Default error action; exact focus not explicit | Error Confirm/Cancel/`pause_menu` receives first | Replaces a prior active error rather than stacking | Dismissal only; no parent terminal | Popup frees and leaves parent stack unchanged | Preserve one topmost error, child-only dismissal, parent state unchanged, and no Pause toggle/open/close from the same input | `Preserve` | `GameInputLifecycleTest`; `SaveLoadDialogTest` |

## Modal-priority matrix

| Priority | Active layer | `pause_menu` | `ui_cancel` | Window close | Explicit action | Result/restoration |
|---:|---|---|---|---|---|---|
| 1 | Child popup or key capture | Child/capture policy | Child/capture policy | Child only | Child only | Parent remains inert |
| 2 | Blocking error or confirmation | Error/confirmation policy | Error/confirmation policy | Topmost only | Safe action only | Invoking parent |
| 3 | Deferred Pause restoration | Consume as no-op | No additional owner | Not applicable | Not applicable | Restoring Pause |
| 4 | Owning modal or world interaction | Owning-flow policy | Owning dialog policy | Owning dialog policy | Owning dialog policy | Its documented parent |
| 5 | Parent screen | Parent policy | Parent policy | Parent policy | Parent policy | Its parent |
| 6 | Gameplay fallback | Open Pause | No duplicate action | Not applicable | Not applicable | Pause |

## Verification evidence

- Implementation-start commit: `git rev-parse HEAD` → `33e9567b04d06bf532ce1d7deb3b6432cb5a927a`.
- Full suite: `dotnet test Sirius.sln --settings test.runsettings.local` → Passed: 863, Failed: 0, Skipped: 0, Total: 863.
- Baseline orphan scan: 9 matching log lines; distinct messages are recorded in the document header.
- The baseline log was captured at `/tmp/hpa-376-test-baseline.log` and is intentionally not committed.

## HPA-378/379 handoff

- `INV-PAUSE`: host-owned Pause parent, paused world, gameplay HUD hidden, visible cursor, Inventory focus/restoration, and current inventory-domain behavior preserved.
- `PAUSE-ROOT`: host-owned paused world, gameplay HUD visible but inert, visible cursor, Resume focus, and existing child/domain cleanup preserved.
- `PAUSE-QUIT-TO-MAIN`: explicit quit-with-risk confirmation with current Pause and gameplay-domain cleanup preserved.
- `SAVE-QUIT-TO-MAIN`: explicit quit-with-risk confirmation with Save/Load, hidden Pause, and gameplay-domain cleanup preserved.
- `REWARD-TOAST`: non-blocking queued presentation, HUD retained, cursor unchanged, no focus or Cancel owner, with producer-owned grant/save semantics.
- `REWARD-BLOCKING`: inert/paused parent as required, competing HUD hidden, visible cursor, Continue focus, required acknowledgement immune to generic Cancel, coordinated with HPA-393.
- `CONFIRM-DESTRUCTIVE`: explicit safe action and explicit destructive confirmation; generic Cancel restores the invoking parent and never confirms.
