# HPA-376 Sirius UI Lifecycle Contract

**Implementation-start commit:** `33e9567b04d06bf532ce1d7deb3b6432cb5a927a`
**Runtime:** Godot 4.6.2, .NET 8.0, GdUnit4
**Baseline result:** Passed: 863, Failed: 0, Skipped: 0, Total: 863.
**Baseline orphan evidence:** 9 orphan-warning lines. Distinct messages: `Detected <7> orphan nodes during test execution!`; `Detected <10> orphan nodes during test execution!`; `Detected <1> orphan nodes during test execution!`.

## Contract policy

Each row records:

1. observed `file:member` evidence;
2. required migration behavior;
3. one disposition: `Preserve`, `Fix in HPA-376`, or `Replace in HPA-378/379`;
4. protecting test evidence or downstream owner.

Unknown or absent focus, cursor, and restoration behavior is stated explicitly rather than inferred from Godot defaults. “HUD retained” means the current gameplay HUD is not explicitly hidden by the cited owner; it does not imply a shared HUD policy.

Configured Cancel has two runtime action surfaces. Settings keeps `pause_menu` mirrored to the effective `ui_cancel` keyboard binding and preserves `ui_cancel` controller events. It then synchronizes those effective events into Godot's `ui_close_dialog` action, which is what `AcceptDialog` consumes. Synchronization removes only exact injected instances it still owns, preserves native `ui_close_dialog` bindings, and replaces stale configured events on every apply. Main-Menu Settings remains a direct `ui_cancel` controller; hosted Inventory terminal `ui_cancel` and `toggle_inventory` actions are dispatched by `UIScreenHost`; `AcceptDialog`-derived owners consume `ui_close_dialog`.

Injected-event ownership is based on the exact `InputEvent` instance currently
stored in `InputMap`, not event equivalence. If another owner reconstructs
`ui_close_dialog` with an equivalent native event, the missing injected
instance is treated as stale ownership and discarded without erasing the
replacement.

## Flow lifecycle matrix

| ID | Observed evidence | Entry and parent | Pause and input block | HUD and cursor | Focus | Input surfaces and topmost receiver | Nested behavior | Terminal signal/count | Cleanup and restoration | Required migration contract | Disposition | Evidence/owner |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `MAIN-ROOT` | `scripts/ui/MainMenu.cs:_Ready` | Startup or return-to-title; no parent screen | Tree pause is not changed; Main Menu buttons own UI input | Main Menu visible; no explicit cursor policy | Scene-authored/default focus is not explicitly restored | Buttons receive explicit actions; no root Cancel handler is observed | Load, Settings, and messages layer above root | No terminal signal; Start changes scene and Quit exits | Root remains until scene change or application exit | Preserve Main Menu as the root; child close restores only Main Menu; root Cancel remains a no-op unless separately approved | `Preserve` | `MainMenuTest.QuitButton_RequestsApplicationQuitOnce` |
| `MAIN-LOAD` | `scripts/ui/MainMenu.cs:_on_load_button_pressed`; `scripts/ui/MainMenu.cs:OnHostedLoadClosed` | Load button from Main Menu; Main Menu is parent | Parent remains present; scene-authored `SaveLoadScreenController` is exclusive; no tree-pause mutation | Main Menu remains beneath the screen; no explicit cursor policy | `SaveLoadScreenController.InitialFocusTarget` is assigned explicitly | Slot/button input goes to Save/Load; ordinary close returns to Main Menu | No-save path opens a `SiriusPromptVariant.Warning` Prompt via `TryOpenMessage`; unavailable-save path opens a `SiriusPromptVariant.RecoverableError` Prompt; load failure while Load is active hosts a `SiriusPromptVariant.RecoverableError` Prompt as a child of the Load entry (`parent: _loadHandle`), keeping Load active until acknowledged | `LoadSlotSelected` or `Closed`; parent cleanup is intended once | `CleanupLoadScreen` disconnects and frees; successful load hands off through `PendingLoadData` | Preserve empty/populated/error routing, successful handoff, explicit initial focus, and restoration to Main Menu; terminal handling must remain idempotent | `Preserve` | `MainMenuTest.LoadUnavailable_OpensRecoverablePromptWithoutAcceptDialog`; `MainMenuTest.HostedLoadFailure_KeepsLoadParentAfterAcknowledge`; `SaveLoadScreenControllerTest.LoadMode_ValidManualSlotEmitsLoadOnce` |
| `MAIN-SETTINGS` | `scripts/ui/MainMenu.cs:_on_settings_button_pressed`; `scripts/ui/MainMenu.cs:OnSettingsClosed` | Settings button from Main Menu; Main Menu is parent | Settings consumes input; inherits unpaused Main Menu state | Main Menu remains beneath full overlay; cursor not explicitly owned | Settings grabs its first control; Main Menu focus restoration is absent | Settings owns Apply, Cancel, and remapped Cancel | Dropdown/key capture remain Settings children | `Closed`; Main Menu currently cleans up on each received emission | `OnSettingsClosed` disconnects, frees, and returns to Main Menu | Preserve staged Apply/discard behavior and Main Menu restoration; controller emission count is owned by `SET-MAIN` | `Preserve` | `MainMenuTest.SettingsPressed_DoesNotStackAndClosedCleansOnlySettingsChild`; `SettingsMenuControllerTest.OpenSettings_ShowsController` (existing) |
| `MAIN-MESSAGE` | `scripts/ui/MainMenu.cs:TryOpenMessage`; `scripts/ui/components/SiriusPrompt.cs` | Information/error over Main Menu (`SiriusPromptVariant.Warning` for the no-save-files message, `SiriusPromptVariant.RecoverableError` for load failures), presented by `UIScreenHost` as a root `UIScreenKinds.Prompt` | No tree-pause mutation; the Prompt uses `Always` processing and `Cancel = Consume`, and `InterceptCancel -> RequestCancel()` maps configured Cancel to the primary action for one-action variants | Main Menu remains below; host cursor policy applies | Prompt primary is the initial focus target for non-destructive variants | The shared Prompt owns Confirm and configured Cancel; `ui_cancel` is consumed by the Prompt and never falls through to Main Menu | May be invoked instead of Load/Settings or as a child of the Load screen (`parent: _loadHandle`) | `PrimaryRequested` routes through `OnMessagePrimaryRequested`, which captures the closure, attempts the close, then invokes the closure regardless of close status; `SiriusPrompt` emits its terminal signal at most once per presentation | Close attempt clears the prompt, `RefreshActionAvailability()` restores root actions, and Main Menu remains usable | Preserve one-action recoverable message behavior, root action blocking while a message is open, and Main Menu restoration | `Preserve` | `MainMenuTest.TryOpenMessageIsLatchedAndTerminalSignalsCloseOnce`; `MainMenuTest.RootPrompt_ConfiguredCancelUsesPromptLatchAndRestoresRoot`; `MainMenuTest.LoadPressed_NoSaveFilesOpensWarningPromptAndRestoresLoadFocus` |
| `MAIN-QUIT` | `scripts/ui/MainMenu.cs:_on_quit_button_pressed` | Explicit Quit from root; no active gameplay progress | No pause change; quits immediately | Main Menu remains until application exit; cursor unspecified | Quit button is the invoking control | Explicit Quit only; root Cancel does not confirm | None | `SceneTree.Quit`; one explicit action | Application exits; no gameplay cleanup is required | Preserve immediate root Quit without quit-with-risk confirmation | `Preserve` | `MainMenuTest.QuitButton_RequestsApplicationQuitOnce` |
| `EXP-GAMEPLAY` | `scripts/game/Game.cs:_EnterTree`; `scripts/game/Game.cs:HandleGameplayRootCancel`; `scenes/game/Game.tscn:UI/UIScreenHost` | Active gameplay; production `Game` owns one local `UIScreenHost`, while synthetic test `Game`s may omit it; hosted views are created unparented and attached by `UIScreenHost`, never pre-parented by `Game` | World continues; the host root fallback opens Pause only when no blocker owns input | Gameplay HUD is retained with no active modal; active host policy owns cursor/HUD | No UI focus; gameplay owns input | Host traversal is topmost; root fallback opens Pause only after blockers decline | Blocking flows pre-empt gameplay in documented priority | No terminal signal | Closing hosted Pause returns through the host to gameplay | Preserve one production host, host-only view attachment, nullable synthetic-test bootstrap, gameplay fallback, and exactly one topmost receiver | `Preserve` | `GameTest.RootCancel_WhenUnblocked_OpensHostedPauseAndOwnsTreePause`; `GameplayPauseHostTest.RootCancel_PauseMenuActionOpensHostedPauseWhenUnblocked` |
| `EXP-PROMPT` | `scripts/game/Game.cs:UpdateInteractionPrompt` | Adjacent treasure, switch, or riddle while exploring | Hidden while battle, NPC, or world-interaction flags are active | Prompt is part of gameplay HUD; cursor unaffected by source | No focus | No input ownership; describes the current `interact` target | Must track each blocking child and floor replacement | No signal | Recomputed on movement/facing and battle, NPC, world, and floor lifecycle boundaries | Hide during every blocking flow and refresh when battle/NPC/world lifecycles end, without stealing focus or input | `Fix in HPA-376` | `GameInputLifecycleTest.InteractionPrompt_HidesDuringBattleAndRestoresAfterEscape`; `GameInputLifecycleTest.ConfiguredKeyboardCancel_ClosesTopmostRiddleAndRestoresGameplay`; `GameTest.Game_OpeningAdjacentTreasureAwardsOnceAndShowsOpenPrompt` (existing) |
| `EXP-FLOOR-TRANSITION` | `scripts/game/Game.cs:OnFloorLoaded`; `scripts/game/Game.cs:OnFloorChanged`; `scripts/game/Game.cs:ContinueSceneChangeAfterUiTeardown` | Stair transition within gameplay; `FloorManager` replaces the floor | No floor modal pause; live gameplay references are rebound and runtime `GridMap` nodes inherit pausable gameplay processing | Gameplay HUD retained; prompt refresh is deferred; cursor unchanged by source | No UI focus | Floor transition owns world replacement; gameplay resumes afterward | Pending save-spawn validation can surface a corrupt-save error | `FloorLoaded` and `FloorChanged` subscriptions; no UI terminal signal | Old grid signals/display are cleaned; prompt/camera setup is deferred; scene replacement waits until `UIScreenHost.PrepareForTeardown() == Complete` | Preserve live HUD, signal rebinding, inherited runtime GridMap processing, and teardown-safe scene replacement | `Preserve` | `GameInputLifecycleTest.FloorReplacement_RebindsGridAndRefreshesPrompt`; `GameplayPauseHostTest.RuntimeGridMapDoesNotRemainExplicitAlways`; `GameplayPauseHostTest.GameSceneHostPrepareForTeardown_ClosesEntryAndRestoresIncomingState` |
| `INV-GAMEPLAY` | `scripts/game/Game.cs:TryOpenInventory`; `scripts/ui/InventoryMenuController.cs:OpenMenu`; `scripts/ui/InventoryMenuController.cs:CloseMenu` | `toggle_inventory` from exploration; the detached reusable Inventory view is presented through `UIScreenHost` | Inventory itself never writes `SceneTree.Paused`; the host owns the direct Inventory root pause policy | Inventory HUD is hidden while active; host policy makes the cursor visible | Host requests Inventory's content-first initial focus target | Host owns terminal `ui_cancel`/toggle handling; the controller does not terminally close itself | No child modal is documented | No terminal signal; host entry state determines whether Inventory is open | Host detaches the external view on close so it can be reused and restores gameplay focus | Preserve host-owned direct Inventory, no controller pause write, hidden HUD while active, content-first focus, parent/gameplay focus restored on close, and detachable external-view lifecycle | `Fix in HPA-376` | `InventoryMenuControllerTest.OpenAndClose_FromRunningTree_DoesNotChangeTreePauseState`; `GameplayPauseHostTest.DirectInventory_HostsDetachesAndReusesTheExternalView` |
| `INV-BLOCKED` | `scripts/game/Game.cs:_Input`; `scripts/ui/hosting/UIScreenHost.cs:TryHandleInput` | Direct `toggle_inventory` while Settings, Save/Load, battle, NPC, or world interaction is active; active Pause is not a blanket block because its explicit Inventory action opens `INV-PAUSE` | Direct requests are rejected by host/controller/reference and interaction-flag guards; the Pause child inherits the existing Pause lease | Active parent HUD/surface remains unchanged | Existing topmost focus remains | The active blocking layer receives direct input; Pause's explicit Inventory action is the only Pause entry path | Active child remains intact | No direct Inventory terminal signal | Nothing changes for a rejected direct request | Preserve rejected direct entry for non-Pause blockers and the explicit host-owned Pause-child exception | `Preserve` | `UIScreenHostInputTest.EntryScopedAction_WithSettingsAboveInventory_HasNoOwner`; `GameInputLifecycleTest.ConfiguredKeyboardCancel_WorldInteractionConsumesWithoutOpeningHostedPause`; `GameInputLifecycleTest.ConfiguredKeyboardCancel_SaveLoadOverwriteDismissesChildThenClosesHostedChild`; `GameplayPauseHostTest.PauseChildInventory_HostsLogicalPauseChildAndRestoresExistingPause` |
| `INV-PAUSE` | `scripts/game/Game.cs:OnHostedPauseInventoryRequested`; `scripts/game/Game.cs:TryOpenInventory` | Inventory requested from active hosted Pause; the host presents the reusable view with the Pause handle as its logical parent | Parent Pause owns the tree-pause lease; Inventory uses `PauseTree = false` and does not acquire a second lease | Inventory HUD is hidden while active; cursor remains host-visible under the paused parent | Host requests Inventory's content-first initial focus target | Hosted Inventory is topmost; its close action affects only the child | Inventory is detached before the same reusable view is later presented under a different logical parent | No terminal signal; host entry state determines whether Inventory is open | Host closes/detaches Inventory and restores the existing Pause entry and focus | Preserve the single Pause lease, child-only close, detached reuse, hidden HUD while active, content-first focus, parent/gameplay focus restored on close, and restoration to the same Pause | `Preserve` | `GameplayPauseHostTest.PauseChildInventory_HostsLogicalPauseChildAndRestoresExistingPause`; `GameplayPauseHostTest.PauseChildInventory_ToggleInventoryClosesOnlyTheHostedChild` |
| `PAUSE-ROOT` | `scripts/game/Game.cs:TryOpenPause`; `scripts/ui/hosting/UIScreenHost.cs:TryPresent`; `scripts/ui/PauseScreenController.cs` | `pause_menu`/root Cancel from exploration presents hosted Pause over gameplay | Final Pause uses `WhenPaused` with `PauseTree = true` and owns the gameplay tree-pause lease; every Pause child keeps `PauseTree = false` | Gameplay HUD remains visible/inert under the host policy; cursor is visible | Host restores/request Resume focus | Host traversal handles configured Cancel and Resume; root input no longer owns a legacy Pause dialog | Inventory, Settings, Save/Load, and return confirmation are logical host children | `ResumeRequested` closes the root Pause entry | Host closes children first, frees Pause, restores leases, and returns to gameplay | Preserve final host-owned Pause, its one tree-pause lease, child lease inheritance, visible HUD/cursor policy, and host-managed cleanup | `Preserve` | `GameTest.RootCancel_WhenUnblocked_OpensHostedPauseAndOwnsTreePause`; `GameplayPauseHostTest.RootPauseOwnsTreePause_AndResumeRestoresIncomingState`; `GameplayPauseHostTest.GameplayProbe_FreezesDuringRootPauseAndResumesAfterConfiguredPauseAction` |
| `PAUSE-SETTINGS` | `scripts/game/Game.cs:TryOpenHostedSettings`; `scripts/game/Game.cs:OnHostedSettingsClosed` | Settings is an unparented view presented as a logical child of the active Pause handle | Pause retains the only tree-pause lease; Settings uses `PauseTree = false` and remains `Always` so it can receive input while paused | `Hud = Inherit`; host keeps the cursor visible | Settings opens its first control; host returns focus to the still-active Pause entry after close | Host reserves Cancel for key capture/dropdown native handling, otherwise its close policy closes only Settings | Dropdown and capture remain Settings children | Settings emits `Closed` once per show cycle | Host frees Settings and leaves the same Pause entry active | Preserve host attachment, one Pause lease, native-input reservation, child-only close, and host focus restoration | `Preserve` | `GameplayPauseHostTest.HostedSettings_HostsLogicalPauseChildAndRestoresExistingPause`; `GameplayPauseHostTest.HostedSettings_RebindingAndPopupReserveCancelForNativeHandlers` |
| `PAUSE-SAVELOAD` | `scripts/game/Game.cs:TryOpenHostedSaveLoad`; `scripts/game/Game.cs:OnHostedSaveLoadClosed` | Scene-authored Save/Load `Control` is presented by `UIScreenHost` with the active Pause handle as logical parent | Pause retains the only tree-pause lease; Save/Load uses `PauseTree = false` and `Always` processing | `Hud = Inherit`; host keeps the cursor visible | Save/Load owns its explicit initial focus target; host restores the active Pause focus after child close | Host stack order closes a nested child first, then closes only Save/Load for configured Cancel; no Save/Load-specific interception is needed | Overwrite and recoverable errors remain above Save/Load as one shared `UIScreenKinds.Prompt` kind in `UIScreenExclusiveGroups.BlockingPrompt`; overwrite uses `SiriusPromptVariant.DestructiveConfirmation` (primary intentionally `SiriusThemeTypes.DestructiveButton`, normalized from the retired confirmation's WarningButton), errors use `SiriusPromptVariant.RecoverableError`; both retain the Save/Load parent and stay dismissible while the tree is paused (`Always` processing) | A successful save/load operation, `Closed`, or Main Menu request is terminal for that show cycle; a failed save/load operation is not terminal — Save/Load stays active beneath the recoverable-error Prompt and rearms (`SaveLoadScreenController.RearmAfterFailedTerminal`) when the Prompt's `OK` is acknowledged; Prompt terminal signals route through Game's root handler, which captures the closure, attempts the close, then invokes the closure regardless of close status | Host frees the Control and leaves the same Pause entry active unless a selected load or Main Menu request starts teardown-safe scene replacement | Preserve scene-authored Control attachment, one Pause lease, child-first Cancel, parent retention for overwrite/error children, operation routing, and parent retention | `Preserve` | `GameplayPauseHostTest.HostedSaveLoad_SaveAndLoadHostLogicalPauseChildrenAndRestoreExistingPause`; `GameplayPauseHostTest.HostedOverwrite_UsesSharedPromptAndCancelRestoresSaveLoad`; `GameplayPauseHostTest.HostedSaveLoad_LoadFailureKeepsSaveLoadAndHostsPrompt`; `GameInputLifecycleTest.ConfiguredKeyboardCancel_SaveLoadOverwriteDismissesChildThenClosesHostedChild` |
| `PAUSE-QUIT-TO-MAIN` | `scripts/game/Game.cs:OnHostedPauseReturnToTitleRequested`; `scripts/game/Game.cs:TryOpenHostedPrompt`; `scripts/game/Game.cs:ReturnToMainMenu` | Pause Return to Title opens a `SiriusPromptVariant.DestructiveConfirmation` as a blocking logical child of Pause (shared `UIScreenKinds.Prompt` kind in `UIScreenExclusiveGroups.BlockingPrompt`) | Pause retains the tree-pause lease; the confirmation uses `PauseTree = false` and `Always` processing | `Hud = Inherit`; host keeps the cursor visible | Cancel is the confirmation's initial safe focus | Cancel closes only the confirmation and returns to the same Pause; only Return to Title starts navigation | One topmost confirmation; the prompt terminal latch plus `RequestSceneChange`'s one-shot guard make navigation exactly once per presentation | `PrimaryRequested` routes through the root handler: capture, close attempt, then invoke the `ReturnToMainMenu` closure regardless of close status | Cancel frees the child and retains Pause; confirmation routes through `RequestSceneChange`, which waits for `PrepareForTeardown() == Complete` | Preserve safe Cancel focus, child-only Cancel, explicit destructive navigation, one-shot terminal behavior, and teardown-safe transition | `Preserve` | `GameplayPauseHostTest.PauseReturnToTitle_UsesSharedPromptAndCancelRestoresPause`; `GameplayPauseHostTest.PauseReturnToTitle_PrimaryRequestsNavigationOnce` |
| `PAUSE-CHILD-RESTORATION` | `scripts/ui/hosting/UIScreenHost.cs:TryClose`; `scripts/ui/hosting/UIScreenFocusCoordinator.cs`; `scripts/game/Game.cs:TryCloseHostedSettings`; `scripts/game/Game.cs:TryCloseHostedSaveLoad` | A hosted Pause child closes while Pause remains its active logical parent | Pause keeps its existing tree-pause lease; closing a child does not create or restore another lease | Parent HUD/cursor policy remains in force | Host focus restoration returns to the active Pause entry | The closing child consumes its terminal input; no root fallback runs for the same action | Settings, Save/Load, Inventory, and return confirmation all use the same host close transaction | Child terminal signal or host close policy | Host releases the child and restores focus to the retained Pause entry | Preserve direct host-child closure and focus restoration without a separate Pause stack state | `Preserve` | `GameplayPauseHostTest.PauseChildInventory_HostsLogicalPauseChildAndRestoresExistingPause`; `GameplayPauseHostTest.HostedSettings_HostsLogicalPauseChildAndRestoresExistingPause`; `GameplayPauseHostTest.HostedSaveLoad_SaveAndLoadHostLogicalPauseChildrenAndRestoreExistingPause` |
| `SET-MAIN` | `scripts/ui/SettingsMenuController.cs:OpenSettings`; `scripts/ui/SettingsMenuController.cs:OnApplyPressed`; `scripts/ui/SettingsMenuController.cs:OnCancelPressed` | Settings under Main Menu | Inherits unpaused parent; Settings consumes non-navigation input | Full overlay shown; parent inert; cursor not explicitly owned | Master-volume slider receives focus | Apply/Cancel/remapped Cancel own terminal input | Dropdown and key capture pre-empt root Settings | `Closed`; guarded exactly once per open cycle | Main Menu frees controller on first signal | Emit `Closed` exactly once per open cycle for Apply or discard; restore only Main Menu and retain staged-edit semantics | `Fix in HPA-376` | `SettingsMenuControllerTest.CancelAfterStagedEdit_DoesNotMutateSnapshot_AndEmitsClosedOnce`; `SettingsMenuControllerTest.ReopenAfterCancel_AllowsOneNewClosedEmission` |
| `SET-PAUSE` | `scripts/ui/SettingsMenuController.cs:OpenSettings`; `scripts/game/Game.cs:TryOpenHostedSettings`; `scripts/game/Game.cs:OnHostedSettingsClosed` | Settings is a logical child of active hosted Pause | Inherits Pause's tree-pause lease with `PauseTree = false`; host owns terminal navigation | Parent Pause remains active/inert; `Hud = Inherit` and cursor-visible host policy stay in force | Master-volume slider receives focus; host restores Pause focus after close | Apply/Cancel/remapped Cancel close the hosted child unless key capture/dropdown reserves the event | Dropdown and capture pre-empt Settings terminal close | Per-open `_closedEmitted` and `EmitClosedOnce` guard Apply, Cancel, and competing/repeated terminal paths so `Closed` emits exactly once | Host frees Settings and retains the same Pause parent | Emit `Closed` exactly once per open cycle and close only Settings while retaining Pause and its lease | `Fix in HPA-376` | `SettingsMenuControllerTest.CancelAfterStagedEdit_DoesNotMutateSnapshot_AndEmitsClosedOnce`; `GameplayPauseHostTest.HostedSettings_HostsLogicalPauseChildAndRestoresExistingPause` |
| `SET-DROPDOWN` | `scripts/ui/SettingsMenuController.cs:IsPopupOpen`; `scripts/ui/SettingsMenuController.cs:_Input` | Resolution/difficulty `OptionButton` under Settings in either parent | Inherits Settings state; popup is topmost | Settings remains visible; cursor unchanged by source | Popup owns temporary focus by Godot; no extra fallback inferred | `ui_cancel`/Pause reaches the open popup before Settings | Closing popup leaves Settings active and staged edits intact | No Settings terminal signal | Popup closes; Settings remains | Preserve child-first Cancel, no Settings close, no gameplay/Pause fallthrough, and return to Settings controls | `Preserve` | `SettingsMenuControllerTest.EscapeKey_WhenResolutionPopupOpen_DoesNotCloseSettings` (existing); `SettingsMenuControllerTest.EscapeKey_WhenDifficultyPopupOpen_DoesNotCloseSettings` (existing) |
| `SET-CAPTURE` | `scripts/ui/SettingsMenuController.cs:_Input`; `scripts/ui/SettingsMenuController.cs:CancelKeyCapture` | Ordinary key capture inside Settings | Settings remains active and blocks gameplay input | Settings HUD state unchanged; cursor unchanged by source | Invoking binding button remains the logical target | Capture receives candidate, cancel, duplicate, and reserved bindings before Settings | Error label is local feedback, not a modal | No `Closed`; capture ends once on accept/cancel | Button text/error reset; Settings remains active | Preserve valid accept, duplicate/reserved rejection, edited-Pause-key Cancel, and return to the same Settings parent | `Preserve` | `SettingsMenuControllerTest.InputDuringCapture_EscapeKey_CancelsAndRestoresPreviousText` (existing); `SettingsMenuControllerTest.InputDuringCapture_DuplicateKey_ShowsErrorAndKeepsCapture` (existing); `SettingsMenuControllerTest.InputDuringCapture_ReservedKey_ShowsError` (existing) |
| `SET-CAPTURE-PAUSE` | `scripts/ui/SettingsMenuController.cs:ShouldCancelOrClose`; `scripts/ui/SettingsMenuController.cs:IsReservedKey` | Capture of `pause_menu` inside Settings | Settings stays active; Escape/Pause is data, not generic close | Settings remains visible; cursor unchanged by source | Pause binding button is capture target | Capture is topmost; Escape is accepted as candidate | Error feedback remains local | No `Closed`; accepted candidate ends capture once | Binding text updates; Settings remains | Preserve Pause-binding exception across remapped/unbound keyboard and controller paths; do not close Settings or open Pause | `Preserve` | `SettingsMenuControllerTest.InputDuringCapture_PauseMenuEscape_AcceptsBinding` (existing); `SettingsMenuControllerTest.JoypadUiCancel_DoesNotCloseWhenCapturingPauseMenu` (existing) |
| `SAVE-MAIN` | `scripts/ui/SaveLoadScreenController.cs`; `scripts/ui/MainMenu.cs` | Scene-authored Save/Load screen under Main Menu; Main Menu is parent | Inherits unpaused state; exclusive screen blocks parent | Main Menu retained beneath screen; cursor not explicitly owned | `SaveLoadScreenController.InitialFocusTarget` is assigned by the host | Slot buttons, Cancel, and Main Menu request target Save/Load; host policy owns configured Cancel | Overwrite confirmation is higher priority only in Save mode | `Closed`, load/save selection, or Main Menu request; screen terminal gate makes outcomes mutually exclusive | Caller disconnects/frees and restores Main Menu or transitions | Accept configured keyboard/controller Cancel through host policy and emit exactly one mutually exclusive terminal signal per show cycle; child Cancel must not close parent; restore Main Menu | `Fix in HPA-384` | `SaveLoadScreenControllerTest.Close_EmitsOnce`; `SaveLoadScreenControllerTest.SaveMode_MainMenu_EmitsOnce`; `MainMenuTest.LoadPressed_HostsSceneAuthoredLoadScreen` |
| `SAVE-PAUSE` | `scripts/ui/SaveLoadScreenController.cs`; `scripts/game/Game.cs:TryOpenHostedSaveLoad`; `scripts/game/Game.cs:TryCloseHostedSaveLoad` | Save/Load is a host-managed logical child of active Pause | Inherits Pause's tree-pause lease with `PauseTree = false`; the scene-authored Control remains processable while paused | Pause stays active beneath the child; `Hud = Inherit` and cursor-visible host policy remain in force | Slot focus is explicitly provided by `SaveLoadScreenController.InitialFocusTarget`; host restores Pause focus after child close | Host stack order closes the overwrite Prompt child first, then Cancel targets hosted Save/Load | Overwrite/error Prompts remain topmost | Exactly one operation, `Closed`, or Main Menu request is emitted through the screen terminal gate | Host frees Save/Load and retains Pause on close; a failed save/load keeps Save/Load active beneath the recoverable-error Prompt and the screen rearms after acknowledgement; load/Main Menu routes invoke teardown-safe scene replacement | Accept configured keyboard/controller Cancel through host stack order, emit exactly one mutually exclusive terminal signal, and retain the active Pause lease through child cleanup | `Fix in HPA-384` | `SaveLoadScreenControllerTest.Close_EmitsOnce`; `GameInputLifecycleTest.ConfiguredControllerCancel_ClosesHostedSaveLoadBeforePause`; `GameplayPauseHostTest.HostedSaveLoad_SaveAndLoadHostLogicalPauseChildrenAndRestoreExistingPause` |
| `SAVE-OVERWRITE` | `scripts/game/Game.cs:OnHostedOverwriteRequested`; `scripts/game/Game.cs:TryOpenHostedPrompt`; `scripts/ui/components/SiriusPrompt.cs` | Existing valid manual slot; the shared `SiriusPrompt` (`SiriusPromptVariant.DestructiveConfirmation`) is presented by `UIScreenHost` as a `UIScreenKinds.Prompt` in `UIScreenExclusiveGroups.BlockingPrompt` with the active hosted Save/Load handle as logical parent | Inherits the Save/Load/Pause lease; `PauseTree = false` and `Always` processing keep it dismissible while paused | Parent Save/Load remains beneath the child; `Hud = Inherit`; host keeps cursor visible | Cancel is the confirmation's initial focus target (`InitialFocusTarget`) | The overwrite Prompt is a real host child with `BlockingPrompt`; host stack order gives it child-first Cancel (`Cancel = Consume`) and closes only the confirmation | One child only; overwrite primary is intentionally `SiriusThemeTypes.DestructiveButton`, normalized from the retired confirmation's WarningButton | `PrimaryRequested`/`CancelRequested` route through Game's root handler: capture, close attempt, then invoke the closure regardless of close status; Confirm invokes the existing `OnHostedSaveSlotSelected` save path once; Cancel emits no parent terminal | Host queues the child and restores Save/Load; parent remains open on Cancel | Preserve shared Prompt child-only Cancel, explicit overwrite, one-shot terminal behavior, DestructiveButton primary, and return to the invoking Save/Load screen | `Preserve` | `GameplayPauseHostTest.HostedOverwrite_UsesSharedPromptAndCancelRestoresSaveLoad`; `GameplayPauseHostTest.HostedOverwrite_PrimaryInvokesSaveOnce`; `GameplayPauseHostTest.HostedOverwrite_ActiveChildConsumesCancelBeforeSaveLoad`; `GameInputLifecycleTest.ConfiguredKeyboardCancel_SaveLoadOverwriteDismissesChildThenClosesHostedChild` |
| `SAVE-CORRUPT` | `scripts/game/Game.cs:ShowCorruptedSaveError`; `scripts/game/Game.cs:TryOpenHostedPrompt`; `scripts/ui/SaveLoadScreenController.cs:ApplySlotPresentation` | Corrupted-save detection at Game initialization or floor load surfaces a root `SiriusPromptVariant.RecoverableError` Prompt ("Load Failed", primary "Return to Title") | `BlockGameplayInput = true` through the host lease only; Game keeps processing input and the tree is never paused (`PauseTree = false`) | Host cursor policy applies; no native dialog overlay | Prompt primary is the initial focus target | The Prompt owns Confirm and configured Cancel (`Cancel = Consume`, `InterceptCancel -> RequestCancel()` maps Cancel to the primary action); Return to Title routes through `ReturnToMainMenu` | One topmost Prompt; `_hasShownCorruptedSaveError` is a one-shot latch, so a second detection opens no second prompt | `PrimaryRequested`/mapped Cancel route through the root handler: capture, close attempt, then invoke the closure regardless of close status; exactly one Main Menu request per detection | Prompt root teardown clears the entry and the gameplay block; if the host open attempt fails, `GD.PushError` plus `ReturnToMainMenu()` still navigates | Preserve disabled corrupt-load behavior, one topmost recoverable error, host-blocked gameplay without tree pause, one-shot presentation, and mandatory title fallback | `Preserve` | `SaveLoadScreenControllerTest.LoadMode_CorruptedDisabledWithReadableReason`; `GameTest.CorruptedSave_OpensRootRecoverablePromptAndBlocksGameplayWithoutTreePause`; `GameTest.CorruptedSave_PresentationFailureStillReturnsToTitle` |
| `SAVE-ERROR` | `scripts/game/Game.cs:ShowSaveError`; `scripts/game/Game.cs:TryOpenHostedPrompt`; `scripts/ui/components/SiriusPrompt.cs` | Ordinary save/load operation error; the shared `SiriusPrompt` (`SiriusPromptVariant.RecoverableError`, `OK` primary) is presented by `UIScreenHost` as a `UIScreenKinds.Prompt` in `UIScreenExclusiveGroups.BlockingPrompt` with the active hosted Save/Load handle as logical parent | The active Save/Load/Pause lease is unchanged; the Prompt uses `PauseTree = false` and `Always` processing and stays dismissible while paused | Active parent remains below; `Hud = Inherit`; cursor policy is unchanged by the error owner | `OK` primary is the initial focus target | The Prompt owns Confirm and configured Cancel; `Cancel = Consume` and `InterceptCancel -> RequestCancel()` map configured Cancel to the `OK` primary for the one-action variant; dismissal never toggles/opens/closes Pause | One topmost Prompt; a second error while one is active is refused (`TryOpenHostedPrompt` one-shot/`IsKindActive(Prompt)` guard), never stacked or replaced | `PrimaryRequested`/`CancelRequested` route through Game's root handler: capture, close attempt, then invoke the closure regardless of close status; dismissal has no parent terminal signal | Host closes the Prompt entry and leaves the active Save/Load/Pause parent unchanged | Preserve topmost recoverable-error routing and exactly one cleanup; dismiss only the error and never toggle/open/close Pause as a side effect | `Preserve` | `GameplayPauseHostTest.HostedSaveLoad_LoadFailureKeepsSaveLoadAndHostsPrompt`; `GameplayPauseHostTest.HostedOverwrite_PrimaryWithNpcInteractionOpensErrorPromptUnderSaveLoad`; `GameInputLifecycleTest.ConfiguredKeyboardCancel_PausedRecoverablePromptRemainsDismissible`; `GameTest.ShowSaveError_WithoutHostedSaveLoadReturnsFalseAndDoesNotOpenPrompt` |
| `SAVE-QUIT-TO-MAIN` | `scripts/ui/SaveLoadScreenController.cs:OnMainMenuPressed`; `scripts/game/Game.cs:OnHostedSaveLoadMainMenuRequested`; `scripts/game/Game.cs:ReturnToMainMenu` | Main Menu button in hosted in-game Save/Load; Save/Load is a child of active Pause until teardown | The Pause lease remains active until the host teardown transaction closes its entries | Current hosted layers remain valid until `RequestSceneChange` prepares their teardown | Main Menu button invokes | `MainMenuRequested` starts the teardown-safe title transition; this path is distinct from the implemented Pause-root return confirmation | Save/Load may have an overwrite/error Prompt child, which remains topmost before the Main Menu action | `MainMenuRequested` only; deliberately not `Closed` | `PrepareForTeardown()` closes hosted Save/Load and Pause before the scene change completes | HPA-378/379 retains the separate decision to require a Save/Load-specific confirmation; any such child must preserve host parentage, the Pause lease, safe focus, and explicit destructive navigation | `Replace in HPA-378/379` | HPA-378/379: Save/Load-specific confirmation policy, hosted-child teardown, and explicit navigation |
| `BATTLE-PREP` | `scripts/game/Game.cs:OnBattleStarted`; `scripts/ui/hosting/UIScreenHost.cs:TryPresent`; `scripts/ui/BattleManager.cs:StartBattle`; `scripts/ui/BattleManager.cs:InitialFocusTarget` | Encounter creates an unparented Battle `Control` and presents it through `UIScreenHost` as `UIScreenKinds.Battle`; gameplay is the logical parent | Host policy sets `PauseTree = false` and `BlockGameplayInput = true`; timer is stopped until Start | Host sets `Hud = Hidden`, `Cursor = Visible`, and hides lower layers | `InitialFocusTarget` explicitly returns the scene-authored Start button | Start, optional item/page/clear controls, and host-intercepted Cancel (`RequestCancel`) own input | Optional pre-battle consumable selection is local to Preparation; no child screen is opened | Start enters Automatic Combat without a terminal signal; preparation Cancel emits `BattleFinished(false, true)` exactly once and never creates a `BattleResultSummary` | Cancel stops Battle runtime and requests host dismissal; host cleanup queues the view, Game clears battle state, retains the enemy, and restores gameplay | Preserve hosted Preparation, explicit Start focus, optional pre-battle consumables, immediate escape without Results or summary, and host-owned restoration | `Preserve` | `BattleManagerTest.RequestCancelDuringPreparation_EmitsEscapeAndDismissOnce` |
| `BATTLE-AUTO` | `scripts/game/Game.cs:OnBattleStarted`; `scripts/ui/BattleManager.cs:OnStartButtonPressed`; `scripts/ui/BattleManager.cs:OnBattleTurnTimer`; `scripts/ui/BattleManager.cs:RequestCancel` | Start transitions the same hosted Battle entry from Preparation to Automatic Combat | Host keeps `PauseTree = false` and blocks gameplay; the existing 1.5-second timer drives AP scheduling and Battle remains processable | Host keeps the Battle surface visible with HUD hidden and cursor visible | `InitialFocusTarget` returns Escape in combat or the first available Cure control while the local overlay is open | Timer/AP combat, Cure, Escape, and host-intercepted Cancel own input | Cure is a local child overlay: opening stops the timer, and child-first Cancel closes Cure before Battle escape and resumes combat | Victory/defeat resolve their Results and emit the matching `BattleFinished(..., false)` once; escape emits `(false, true)` once without a summary or Results | Escape stops the timer, clears transient effects, kills visual tweens, and requests host dismissal; victory/defeat remain hosted under their result contracts | Preserve timer/AP automatic combat, Cure child-first Cancel, exactly-once terminal signals, and host-owned teardown | `Preserve` | `BattleManagerTest.RequestCancelDuringAutomaticCombat_StopsTimerClearsEffectsAndDismissesOnce`; `GameplayPauseHostTest.Battle_HostsAsBlockingScreenWithoutPausingTree` |
| `BATTLE-ESC-PREP` | `scripts/ui/BattleManager.cs:RequestCancel`; `scripts/game/Game.cs:OnBattleFinished`; `scripts/ui/hosting/UIScreenHost.cs:TryClose` | Host-intercepted Cancel before Start escapes the hosted Battle Preparation screen | Battle remains the blocking owner until its synchronous escape signal is handled; the tree is never paused | Host keeps HUD hidden and cursor visible through the close transaction, then restores gameplay policy | Start receives initial focus; preparation controls remain reachable; no post-escape focus is restored | Host Cancel interception routes to `RequestCancel`; no Pause fallback can consume the same action | Any selected pre-battle item rail is discarded; no child survives dismissal | Exactly one `BattleFinished(false, true)`; no Results phase, reward grant, or `BattleResultSummary` is created | `RequestCancel` stops runtime and requests host close; Game clears `IsInBattle`, retains the enemy, refreshes the prompt, and restores gameplay | Preserve pre-start escape, immediate host dismissal, no Results/summary/reward or enemy removal, no Pause fallthrough, and idempotent cleanup | `Preserve` | `BattleManagerTest.RequestCancelDuringPreparation_EmitsEscapeAndDismissOnce`; `GameInputLifecycleTest.ConfiguredCancel_DuringHostedBattleEscapesWithoutOpeningPause` |
| `BATTLE-ESC-ACTIVE` | `scripts/ui/BattleManager.cs:RequestCancel`; `scripts/ui/BattleManager.cs:StopBattleRuntime`; `scripts/game/Game.cs:OnBattleFinished` | Escape during Automatic Combat is handled by the hosted Battle before gameplay fallback | Timer stops before the escape signal; host continues to block gameplay until dismissal, with `PauseTree = false` | HUD remains hidden and cursor visible until host cleanup; gameplay presentation returns afterward | No post-escape focus target is reachable | Cure overlay consumes Cancel first; otherwise Escape or host Cancel calls `RequestCancel`; Pause never opens | Cure closes locally without escaping; active combat has no other child screen | Exactly one `BattleFinished(false, true)`; escape never enters Results, creates a summary, grants rewards, or removes the enemy | Runtime timer, buffs/status effects, and visual tweens are cleaned; host disconnects/queues Battle and Game restores battle state and prompt | Preserve immediate escape, Cure child-first handling, timer/effect/tween cleanup, enemy retention, no Results/summary/rewards, and exactly-once emission | `Preserve` | `BattleManagerTest.RequestCancelDuringAutomaticCombat_StopsTimerClearsEffectsAndDismissesOnce`; `GameInputLifecycleTest.InteractionPrompt_HidesDuringBattleAndRestoresAfterEscape` |
| `BATTLE-RESULT-VICTORY` | `scripts/ui/BattleManager.cs:EndBattle`; `scripts/ui/BattleManager.cs:RenderResult`; `scripts/game/Game.cs:OnBattleFinished`; `scripts/game/Game.cs:OnBattleDismissRequested` | Victory resolves inside the hosted Battle; Game clears domain battle state while the Battle Results surface remains hosted | Host continues `BlockGameplayInput = true` with `PauseTree = false` after `IsInBattle` clears | Results remain visible with HUD hidden and cursor visible; gameplay stays inert underneath | `InitialFocusTarget` and `RenderResult` explicitly focus Continue | Continue or host-intercepted Cancel requests dismissal; neither re-emits `BattleFinished` or opens Pause | `ResolvedResult` is one read-only `BattleResultSummary`; its already-awarded loot is rendered locally, with no child reward screen | XP/gold/level/skills/loot and enemy removal occur once; then `BattleFinished(true, false)` emits once before explicit dismissal | Continue/dismissal asks the host to close; host cleanup disconnects and queues Battle, restores HUD/cursor/gameplay, and cannot regrant rewards | Preserve one-time victory grants, one resolved summary, hosted Results until explicit dismissal, guarded result emission, and restoration without Pause fallthrough | `Fix in HPA-376` | `BattleManagerTest.Victory_EmitsOnceAndLeavesControlVisibleUntilDismissRequested`; `GameplayPauseHostTest.BattleVictory_RemainsHostedAfterBattleFinishedUntilDismissal` |
| `BATTLE-RESULT-DEFEAT` | `scripts/ui/BattleManager.cs:EndBattle`; `scripts/ui/BattleManager.cs:RenderResult`; `scripts/game/Game.cs:OnBattleFinished`; `scripts/game/Game.cs:ScheduleDefeatReturnToMainMenu` | Defeat resolves hosted Results; Game clears `IsInBattle` and owns the approved delayed return-to-title navigation, whose teardown closes the host | Host remains non-pausing and gameplay-blocking while Results is retained; the delayed return timer is owned by Game and canceled on Game teardown | Defeat Results remain visible with HUD hidden and cursor visible until the delayed title transition tears down the host | No initial focus target; Continue is hidden and disabled during defeat Results | The hosted Battle remains topmost and blocking; host-intercepted Cancel is consumed while defeat Results stays hosted, neither dismissing Battle nor falling through to Pause | `ResolvedResult` is read-only with zero XP, gold, and loot; no reward child or second grant path exists | Defeat renders Results and emits `BattleFinished(false, false)` exactly once; navigation is scheduled at most once and preserves the existing delay | Game invalidates stale delayed callbacks on cleanup; the title transition calls host teardown, disconnects/queues Battle, and restores title state | Preserve zero-reward defeat summary, exactly-once signal, hosted Results lifetime, and the approved delayed return-to-title behavior without adding a new dismissal contract | `Fix in HPA-376` | `GameInputLifecycleTest.DefeatReturnTimerIsOwnedAndDoesNotNavigateAfterCleanup`; `GameInputLifecycleTest.DefeatReturnTimer_NavigatesOnceWhileOwnerLives` |
| `BATTLE-RESULT-ESCAPE` | `scripts/ui/BattleManager.cs:RequestCancel`; `scripts/ui/BattleManager.cs:StopBattleRuntime`; `scripts/game/Game.cs:OnBattleFinished` | Escape is an immediate hosted dismissal from Preparation or Automatic Combat; it never enters a player-visible Results phase | Host handles the escape while blocking gameplay, then releases the block after dismissal without pausing the tree | HUD is hidden and cursor visible only for the active host entry; gameplay restores after close | No post-escape Results or Continue focus exists | Cure consumes Cancel first; otherwise the host routes Cancel to Battle and consumes it, with no second receiver | No reward/summary child and no escape-result presentation | Exactly one `BattleFinished(false, true)`; no `BattleResultSummary`, reward resolution, Results render, enemy removal, or later terminal signal | The same synchronous request stops runtime, clears effects/tweens, emits once, and asks host cleanup to queue the view | Preserve immediate escape, no Results/summary/rewards/enemy removal, child-first Cure handling, and exactly-once host cleanup | `Preserve` | `BattleManagerTest.RequestCancelDuringPreparation_EmitsEscapeAndDismissOnce`; `BattleManagerTest.RequestCancelDuringAutomaticCombat_StopsTimerClearsEffectsAndDismissesOnce` |
| `BATTLE-CLEANUP` | `scripts/game/Game.cs:ClearBattlePresentation`; `scripts/game/Game.cs:OnBattleDismissRequested`; `scripts/game/Game.cs:CleanupBattleManager`; `scripts/ui/BattleManager.cs:StopBattleRuntime`; `scripts/ui/BattleManager.cs:_ExitTree`; `scripts/ui/hosting/UIScreenHost.cs:TryClose` | Any Battle escape, victory/defeat dismissal, host teardown, or Game exit is finalized by host ownership | Host releases the blocking entry and restores the incoming non-paused state; Game clears battle state on `BattleFinished`, while defeat navigation owns its delayed teardown | Host restores HUD/cursor/lower-layer policy to gameplay or title; prompt/player UI are refreshed before gameplay resumes | Host releases Battle focus and restores the lower owner or title; no stale Battle focus remains | `DismissRequested`, `HostTeardown`, and repeated close paths converge through the host transaction; guarded signal/dismiss flags prevent fallthrough or duplication | Cure is local and disappears with Battle; the host owns the screen record and `QueueFree` lifetime | `BattleFinished` is emitted at most once per battle; victory/defeat/escape have distinct payloads and dismissal never re-emits the result | Host cleanup disconnects signals and clears handles; `StopBattleRuntime` stops timer, clears transient buffs/status effects, and kills visual tweens; Game cancels stale defeat callbacks and restores gameplay/title, with idempotent fallback cleanup | Preserve host-owned/idempotent disconnect/free and teardown, timer/effect/tween cleanup, exact-once battle state reset, and correct gameplay/title restoration | `Preserve` | `BattleManagerTest.RequestCancelDuringPreparation_EmitsEscapeAndDismissOnce`; `BattleManagerTest.RequestCancelDuringAutomaticCombat_StopsTimerClearsEffectsAndDismissesOnce`; `GameInputLifecycleTest.DefeatReturnTimerIsOwnedAndDoesNotNavigateAfterCleanup` |
| `NPC-DIALOGUE` | `scripts/ui/NpcInteractionController.cs:Begin`; `scripts/ui/DialogueScreenController.cs:TryStartDialogue`; `scripts/game/Game.cs:OnNpcInteracted` | NPC interaction opens Dialogue; `NpcInteractionController` owns the host handle and presents the scene-authored `scenes/ui/DialogueScreen.tscn` through `UIScreenHost.TryPresent` as a `UIScreenKinds.Dialogue` modal; gameplay is the logical parent | `PauseTree = false`; `BlockGameplayInput = true`; `LowerLayers = VisibleInert`, so the world and HUD remain visible and gameplay is inert under the Dialogue | `Hud = Visible`; `Cursor = Visible`; interaction prompt hidden while active | First choice action is the initial focus target (`InitialFocusTarget`) | Configured Cancel routes through `InterceptCancel -> RequestCancel()` with `Cancel = Consume`; choice buttons and `Farewell.` use `SiriusThemeTypes.SecondaryButton`; no native dialog is involved | Dialogue may transition to Shop or Heal through the same interaction owner; the Dialogue-owned `%SafeFrame` sizes the surface (`SiriusModalSizeClass.Full` + `%SafeFrame` produce the wide-bottom surface; Dialogue never writes shell-owned `%Panel` geometry) | `DialogueOutcome` or `DialogueClosed`; the shared `TryBeginTerminal` latch makes the two terminal signals mutually exclusive and at most once per cycle, covering Cancel (`RequestCancel`), `Farewell.`, broken next-node links, and outcomes | Controller closes the hosted entry via `CloseDialoguePresentation(Programmatic)` and transitions or calls `Finish()`; host cleanup runs `ClearDialoguePresentation`; Game restores gameplay and the prompt | Emit exactly one mutually exclusive terminal outcome per cycle; configured Cancel closes only the Dialogue entry, never falling through to Pause; standard height is bounded to the lower 45% safe-frame band while compact may use full safe height with body scrolling; `EndNpcInteractionIfActive()` covers completion/failure/reset/teardown | `Preserve` | `DialogueScreenControllerTest.TryStartDialogue_SecondSuccessfulStartIsRejected`; `DialogueScreenControllerTest.RequestCancelTwice_EmitsDialogueClosedOnce`; `DialogueScreenControllerTest.OutcomeThenCancel_EmitsOutcomeOnly`; `DialogueScreenControllerTest.Scene_UsesSafeFrameModalShellAndContainsNoAcceptDialog`; `DialogueScreenControllerTest.StandardDialogue_StaysWithinLowerBand`; `DialogueScreenControllerTest.CompactDialogue_FillsSafeHeightAndScrollsToFocusedChoice`; `NpcInteractionControllerTest.Begin_HostsOneDialogueEntry`; `NpcInteractionControllerTest.DialogueCancel_ClosesHostedEntryAndCompletesOnce`; `GameplayPauseHostTest.NpcDialogue_HostsAsBlockingScreenWithoutPausingTree`; `GameplayPauseHostTest.NpcDialogue_GoodbyeCompletesThroughRealRoute`; `GameInputLifecycleTest.ConfiguredKeyboardCancel_ClosesHostedDialogueThroughRealRoute`; `GameInputLifecycleTest.ConfiguredControllerCancel_ClosesHostedDialogueThroughRealRoute`; HPA-625 owns the missing HPA-373 portrait requirement; HPA-570 still owns Shop/Heal migration |
| `NPC-TO-SHOP` | `scripts/ui/NpcInteractionController.cs:OnDialogueOutcome`; `scripts/ui/NpcInteractionController.cs:OpenShop` | `OpenShop` outcome from hosted Dialogue; the controller instantiates `scenes/ui/ShopScreen.tscn` and presents it through `TryHostSurface`/`UIScreenHost.TryPresent` as a `UIScreenKinds.Shop` modal; NPC interaction remains the orchestration owner | `PauseTree = false`; `BlockGameplayInput = true` keeps the gameplay block continuously active | Hosted Dialogue entry closes before the hosted Shop entry opens; `Hud = Hidden`, `Cursor = Visible`, `LowerLayers = VisibleInert`; prompt stays hidden | Host `InitialFocus` resolves the Shop screen's explicit `InitialFocusTarget` | Transition consumes the dialogue outcome; Shop becomes the topmost receiver | Missing shop data finishes safely (`Finish()` with no child); a rejected host open frees the candidate and finishes | No interaction terminal during successful transition | Controller closes the hosted Dialogue entry (`CloseDialoguePresentation(Programmatic)`), then hosts the Shop screen; host cleanup runs `ClearShopPresentation` with `QueueFree` node lifetime | Preserve atomic Dialogue-to-Shop replacement, continuous NPC block, safe missing-data cleanup, and the later return target through the same owner | `Preserve` | `NpcInteractionControllerTest.ShopOutcome_ClosesHostedDialogueThenHostsSingleShopEntry`; `NpcInteractionControllerTest.ShopOutcome_InvalidShopId_OpensNoShopAndCompletesOnce` |
| `NPC-SHOP` | `scripts/ui/ShopScreenController.cs:TryOpenShop`; `scripts/ui/ShopScreenController.cs:RequestCancel`; `scripts/ui/NpcInteractionController.cs:OnShopClosed` | Hosted Shop screen (`UIScreenKinds.Shop` modal) under the NPC interaction owner | `PauseTree = false`; `BlockGameplayInput = true` with exclusive Modal input priority; no scene-tree pause | `Hud = Hidden`; `Cursor = Visible`; `LowerLayers = VisibleInert` keeps the world visible and inert | Host `InitialFocus` resolves the screen's explicit `InitialFocusTarget` | Buy/Sell rows, Close button, and host-intercepted Cancel (`Cancel = Consume`, `InterceptCancel -> RequestCancel()`) target Shop | Feedback timer is local transient state | `ShopClosed` guarded once by `_terminalEmitted` | Pending feedback timer is canceled on close; host cleanup (`Cleanup = ClearShopPresentation`, `QueueFree` node lifetime) frees the screen; controller finishes and Game clears the NPC flag and refreshes the prompt | Preserve buy/sell domain behavior, once-only close, feedback-timer cleanup, hidden HUD/visible cursor policy, and restoration to the invoking NPC/gameplay target through the same orchestration owner | `Preserve` | `ShopScreenControllerTest.RequestCancelTwice_EmitsShopClosedOnce`; `ShopScreenControllerTest.RequestCancel_CancelsPendingFeedbackTimer`; `ShopScreenControllerTest.Sell_LastItem_ShowsNothingToSellImmediately`; `GameInputLifecycleTest.ConfiguredKeyboardCancel_ClosesHostedShopThroughRealRoute` |
| `NPC-TO-HEAL` | `scripts/ui/NpcInteractionController.cs:OnDialogueOutcome`; `scripts/ui/NpcInteractionController.cs:OpenHeal` | `Heal` outcome from hosted Dialogue; the controller instantiates `scenes/ui/HealingScreen.tscn` and presents it through `TryHostSurface`/`UIScreenHost.TryPresent` as a `UIScreenKinds.Heal` modal; NPC interaction remains the orchestration owner | `PauseTree = false`; `BlockGameplayInput = true` keeps the gameplay block continuously active | Hosted Dialogue entry closes before the hosted Heal entry opens; `Hud = Hidden`, `Cursor = Visible`, `LowerLayers = VisibleInert`; prompt stays hidden | Host `InitialFocus` resolves the Healing screen's explicit `InitialFocusTarget` | Transition consumes the dialogue outcome; Heal becomes the topmost receiver | None beyond local feedback | No interaction terminal during transition | Controller closes the hosted Dialogue entry (`CloseDialoguePresentation(Programmatic)`), then hosts the Healing screen; host cleanup runs `ClearHealPresentation` with `QueueFree` node lifetime | Preserve atomic Dialogue-to-Heal replacement, continuous NPC block, and restoration through the same interaction owner | `Preserve` | `NpcInteractionControllerTest.HealOutcome_ClosesHostedDialogueThenHostsSingleHealEntry` |
| `NPC-HEAL` | `scripts/ui/HealingScreenController.cs:OnHealPressed`; `scripts/ui/HealingScreenController.cs:RequestCancel`; `scripts/ui/NpcInteractionController.cs:OnHealDone` | Hosted Healing screen (`UIScreenKinds.Heal` modal) under the NPC interaction owner | `PauseTree = false`; `BlockGameplayInput = true` with exclusive Modal input priority; no scene-tree pause | `Hud = Hidden`; `Cursor = Visible`; `LowerLayers = VisibleInert` keeps the world visible and inert; prompt hidden | Host `InitialFocus` resolves the screen's explicit `InitialFocusTarget` | Heal, No Thanks, and host-intercepted Cancel (`Cancel = Consume`, `InterceptCancel -> RequestCancel()`) target Heal | Insufficient gold/full HP is local standing feedback | `HealComplete` or `HealCancelled`; guarded as one mutually exclusive terminal result per open cycle by `_terminalEmitted` | Host cleanup (`Cleanup = ClearHealPresentation`, `QueueFree` node lifetime) frees the screen; controller finishes; Game clears the NPC flag and refreshes the prompt | Emit exactly one mutually exclusive terminal result per open cycle; preserve heal/gold rules; restore through NPC cleanup only once | `Preserve` | `HealingScreenControllerTest.HealThenCancel_EmitsNoCancellation`; `HealingScreenControllerTest.RequestCancelTwice_EmitsCancelledOnce`; `HealingScreenControllerTest.Heal_PressedTwice_SpendsAndEmitsOnce`; `NpcInteractionControllerTest.HealOutcome_ClosesHostedDialogueThenHostsSingleHealEntry` |
| `NPC-CLEANUP` | `scripts/ui/NpcInteractionController.cs:Finish`; `scripts/game/Game.cs:OnNpcInteractionComplete`; `scripts/game/Game.cs:EndNpcInteractionIfActive` | Any Dialogue/Shop/Heal exit, open failure, reset, or teardown | `IsInNpcInteraction` remains until orchestration completes | Active dialog removed; prompt/gameplay HUD refresh through `UpdateInteractionPrompt`/`UpdatePlayerUI`; cursor ownership absent | Gameplay focus fallback is not explicit | Once children are gone, gameplay regains input | Hosted Dialogue/Shop/Heal entries are all cleaned | `InteractionComplete` guarded once by `_finished` | `Finish()` disconnects signals, closes the hosted Dialogue entry, cleans up Shop/Heal, and invokes `InteractionComplete`; Game unsubscribes, then `EndNpcInteractionIfActive()` clears the flag exactly once; reset routes through `NpcInteractionResetRequested -> Finish()` and Game `_ExitTree` teardown calls `Finish()` + `EndNpcInteractionIfActive()` | Preserve idempotent orchestration cleanup and exactly one flag clear across every success, Cancel, reset, and failure path | `Preserve` | `NpcInteractionControllerTest.DialogueCancel_ClosesHostedEntryAndCompletesOnce`; `NpcInteractionControllerTest.Finish_WhileDialogueActive_ClosesHostedEntryAndCompletesOnce`; `NpcInteractionControllerTest.MissingTree_CreatesNoHostedEntryAndCompletesOnce`; `GameplayPauseHostTest.NpcDialogue_TreeExitClearsDomainFlagAndHostEntries`; `GameInputLifecycleTest.ConfiguredKeyboardCancel_ClosesHostedDialogueThroughRealRoute` |
| `WORLD-TREASURE` | `scripts/game/Game.cs:OnTreasureBoxOpenRequested` | Adjacent treasure from exploration | `IsInWorldInteraction` blocks Inventory/Pause during asynchronous opening | Prompt hidden; no standalone reward modal; cursor unchanged by source | No UI focus | Atomic world interaction consumes competing input | No presentation child | No UI terminal signal | `finally` clears world flag and refreshes prompt; grant, opened ID, and cell clear occur once | Preserve grant, opened-state persistence, cell clear, flag cleanup, and silent domain semantics; later UI consumes an already-resolved payload and never grants/reconstructs reward | `Preserve` | `GameTest.Game_OpeningAdjacentTreasureAwardsOnceAndShowsOpenPrompt` (existing); `GameTest.Game_AbortedTreasureOpeningDoesNotGrantRewardOrPersistOpenedId` (existing) |
| `WORLD-SWITCH` | `scripts/game/Game.cs:HandlePuzzleSwitch` | Adjacent puzzle switch from exploration | `IsInWorldInteraction` wraps atomic activation and blocks competing input | No modal; prompt refreshes; cursor unchanged | No focus | Atomic interaction is topmost; Pause is consumed by world block | May arm another puzzle phase but has no child UI | No UI terminal signal | `finally` clears flag and refreshes prompt | Preserve atomic activation result, no modal, exactly-once flag cleanup, and gameplay restoration | `Preserve` | `GameTest.Game_SwitchThenCorrectRiddleSolvesPuzzleOpensGateAndDisablesTrap` (existing); `GameTest.PauseMenu_WhenInWorldInteraction_MarksInputHandledAndDoesNotOpenPauseMenu` (existing) |
| `WORLD-RIDDLE` | `scripts/game/Game.cs:OpenPuzzleRiddle`; `scripts/ui/PuzzleRiddleDialog.cs:OnCloseRequested`; `scripts/game/Game.cs:CleanupPuzzleRiddleDialog` | Riddle modal from exploration; gameplay is parent | `IsInWorldInteraction` blocks gameplay; `Game` lets the dialog receive configured Cancel | Prompt hidden; modal visible; cursor not explicitly owned | Choice focus is not explicitly assigned | Choice, synchronized `ui_close_dialog`, and window close target Riddle | Dormant result can re-render same dialog with message | `ChoiceSelected` or `PuzzleRiddleClosed`; guarded as one mutually exclusive terminal action per presentation cycle | Game disconnects/frees, clears active riddle/flag, and refreshes prompt | Emit exactly one mutually exclusive terminal action per presentation cycle; retain retry behavior; configured Cancel must clean up only the topmost riddle and restore gameplay once | `Fix in HPA-376` | `PuzzleRiddleDialogTest.CanceledThenCloseRequested_EmitsPuzzleRiddleClosedOnce`; `PuzzleRiddleDialogTest.ChoiceThenCancel_EmitsChoiceSelectedOnly`; `GameInputLifecycleTest.ConfiguredKeyboardCancel_ClosesTopmostRiddleAndRestoresGameplay`; `GameTest.Game_WrongRiddleAnswerAppliesPenaltyAndAllowsRetry` (existing) |
| `WORLD-CLEANUP` | `scripts/game/Game.cs:CleanupPuzzleRiddleDialog`; `scripts/game/Game.cs:OnTreasureBoxOpenRequested`; `scripts/game/Game.cs:HandlePuzzleSwitch` | Any treasure, switch, riddle, or failure exit | World flag is the gameplay-input block | Prompt is restored after cleanup; cursor/focus ownership absent | Gameplay focus fallback is not explicit | Gameplay becomes receiver only after active world owner ends | Riddle node/signals and atomic async state must be released | No shared UI terminal signal | Each path clears `IsInWorldInteraction`; riddle cleanup is repeat-safe at Game layer | Preserve exactly-once domain completion, idempotent node cleanup, and prompt/gameplay restoration on success, configured Cancel, and failure | `Preserve` | `GameTest.Game_AbortedTreasureOpeningDoesNotGrantRewardOrPersistOpenedId` (existing); `GameTest.Game_SwitchThenCorrectRiddleSolvesPuzzleOpensGateAndDisablesTrap` (existing); `PuzzleRiddleDialogTest.CanceledThenCloseRequested_EmitsPuzzleRiddleClosedOnce`; `GameInputLifecycleTest.ConfiguredKeyboardCancel_ClosesTopmostRiddleAndRestoresGameplay` |
| `REWARD-TOAST` | `scripts/game/Game.cs:OnTreasureBoxOpenRequested`; no standalone reward surface exists | Future brief single reward from gameplay producer | Current treasure grant is atomic; no toast or pause lifecycle exists | Current grant is silent; target retains gameplay HUD and cursor | No focus | Current flow owns no Cancel surface | Future queue is absent | No current UI terminal signal | Current domain cleanup ends world interaction; no presentation cleanup exists | Queued non-blocking toast: no tree pause, HUD retained, cursor unchanged, no focus or Cancel owner, deterministic queue/lifetime cleanup; producer retains all grant/save authority | `Replace in HPA-378/379` | HPA-378/379 plus downstream reward screen: non-blocking, HUD retained, cursor unchanged, no Cancel owner |
| `REWARD-BLOCKING` | `scripts/ui/BattleManager.cs:EndBattle`; `scripts/ui/BattleManager.cs:ShowPendingLootDisplay`; no shared constellation exists | Future important/multiple/result acknowledgement from producer | Current loot is embedded in Battle; no shared blocking lifecycle exists | Target hides competing HUD, keeps parent inert, and shows cursor | Continue focused when actionable | Required acknowledgement ignores generic Cancel; explicit Continue owns exit | Producer-provided reward nodes/payload; shared child model absent | No shared terminal signal | Current Battle owns grant and cleanup; future presentation restores producer continuation | Blocking constellation pauses/inerts parent as required, uses visible cursor and Continue focus, cannot be Cancel-dismissed when required, restores producer target, and never grants or reconstructs reward | `Replace in HPA-378/379` | HPA-378/379 plus HPA-393 handoff: parent inert, cursor visible, Continue focus, required acknowledgement ignores Cancel |
| `CONFIRM-ORDINARY` | `scripts/game/Game.cs:OnHostedOverwriteRequested`; `scripts/game/Game.cs:TryOpenHostedPrompt`; `scripts/game/Game.cs:ShowSaveError`; `scripts/ui/components/SiriusPrompt.cs` | The shared `SiriusPrompt` scene is presented by `UIScreenHost` as a `UIScreenKinds.Prompt` over the active hosted Save/Load screen; overwrite (`SiriusPromptVariant.DestructiveConfirmation`) and recoverable save/load errors (`SiriusPromptVariant.RecoverableError`) are logical children of the Save/Load handle | `PauseTree = false`; inherits the active Save/Load/Pause lease with `Always` processing; the host owns blocking and input policy | Save/Load remains visible/inert beneath the child; `Hud = Inherit`; host keeps cursor visible | Cancel is the overwrite confirmation's initial focus target; error prompts focus their `OK` primary | Host-stack dispatch gives the child Prompt first refusal; configured Cancel closes only the child and the next Cancel closes Save/Load; `Cancel = Consume` and `InterceptCancel -> RequestCancel()` keep Cancel from falling through | One host-owned Prompt child in `UIScreenExclusiveGroups.BlockingPrompt`; overwrite primary is intentionally `SiriusThemeTypes.DestructiveButton` (normalized from the retired confirmation's WarningButton); errors use the same shared Prompt kind | `PrimaryRequested`/`CancelRequested` route through Game's root handler: capture, close attempt, then invoke the closure regardless of close status; overwrite primary invokes the existing save path; error dismissal has no parent terminal | Host queues the child and restores Save/Load/Pause; error cleanup leaves its parent unchanged | Preserve shared Prompt/`BlockingPrompt` ownership, explicit safe focus, child-only Cancel, no parent fallthrough, and idempotent cleanup | `Preserve` | `GameplayPauseHostTest.HostedOverwrite_UsesSharedPromptAndCancelRestoresSaveLoad`; `GameplayPauseHostTest.HostedOverwrite_PrimaryInvokesSaveOnce`; `GameplayPauseHostTest.HostedOverwrite_PrimaryWithNpcInteractionOpensErrorPromptUnderSaveLoad`; `GameplayPauseHostTest.HostedSaveLoad_LoadFailureKeepsSaveLoadAndHostsPrompt`; `GameplayPauseHostTest.HostedPrompt_ProgrammaticCloseClearsHandleAndGroup`; `GameInputLifecycleTest.ConfiguredKeyboardCancel_SaveLoadOverwriteDismissesChildThenClosesHostedChild`; `GameInputLifecycleTest.ConfiguredKeyboardCancel_PausedRecoverablePromptRemainsDismissible` |
| `CONFIRM-PAUSE-RETURN` | `scripts/game/Game.cs:OnHostedPauseReturnToTitleRequested`; `scripts/game/Game.cs:TryOpenHostedPrompt`; `scripts/game/Game.cs:ReturnToMainMenu` | The shared `SiriusPrompt` (`SiriusPromptVariant.DestructiveConfirmation`) is presented by `UIScreenHost` as a `UIScreenKinds.Prompt` in `UIScreenExclusiveGroups.BlockingPrompt`, a blocking logical child of the active Pause handle before title navigation | Pause keeps the tree-pause lease; the confirmation uses `PauseTree = false` and `Always` processing | Parent remains inert beneath the confirmation; host keeps cursor visible | Cancel is focused by default | Generic Cancel closes only the child and retains Pause; only Return to Title invokes `ReturnToMainMenu`; the Prompt consumes configured Cancel (`Cancel = Consume`, `InterceptCancel -> RequestCancel()`) | One topmost confirmation; the prompt terminal latch plus `RequestSceneChange`'s one-shot guard make navigation exactly once per presentation | Exactly one terminal signal per presentation; `PrimaryRequested` routes through the root handler: capture, close attempt, then invoke the `ReturnToMainMenu` closure regardless of close status | Cancel restores the active Pause entry; confirmation follows the teardown-safe scene change path (`RequestSceneChange` waits for `PrepareForTeardown() == Complete`) | Preserve the implemented safe confirmation, explicit destructive navigation, and one-lease host cleanup | `Preserve` | `GameplayPauseHostTest.PauseReturnToTitle_UsesSharedPromptAndCancelRestoresPause`; `GameplayPauseHostTest.PauseReturnToTitle_PrimaryRequestsNavigationOnce`; `GameplayPauseHostTest.RequestSceneChange_OneShotGuardSuppressesRepeatCommit` |
| `ERROR-TOPMOST` | `scripts/game/Game.cs:ShowSaveError`; `scripts/game/Game.cs:TryOpenHostedPrompt`; `scripts/game/Game.cs:HandleGameplayRootCancel` | Recoverable-error Prompt over the active hosted Save/Load child (under Pause in gameplay); the error pre-empts host Pause fallback without changing the active parent pause lease | Parent remains below with its lease unchanged; the Prompt uses `PauseTree = false` and `Always` processing | Parent remains below; cursor policy is unchanged by the error owner | `OK` primary is the default error action; exact focus not inferred | The error Prompt owns Confirm and configured Cancel and receives first; it never falls through to Pause or gameplay | One topmost Prompt; a second error while one is active is refused rather than stacked or replaced | Dismissal only; no parent terminal | Host closes the Prompt entry and leaves the parent stack unchanged | Preserve one topmost error, child-only dismissal, parent state unchanged, and no Pause toggle/open/close from the same input | `Preserve` | `GameInputLifecycleTest.ConfiguredKeyboardCancel_PausedRecoverablePromptRemainsDismissible`; `GameInputLifecycleTest.ConfiguredKeyboardCancel_RecoverablePromptDoesNotFallThroughToParents`; `GameTest.ShowSaveError_WithoutHostedSaveLoadReturnsFalseAndDoesNotOpenPrompt` |

## Modal-priority matrix

| Priority | Active layer | `pause_menu` | Direct `ui_cancel` / dialog `ui_close_dialog` | Window close | Explicit action | Result/restoration |
|---:|---|---|---|---|---|---|
| 1 | Child popup or key capture | Child/capture policy | Child/capture policy | Child only | Child only | Parent remains inert |
| 2 | Blocking error or confirmation | Error/confirmation policy | Error/confirmation policy | Topmost only | Safe action only | Invoking parent |
| 3 | Owning modal or world interaction | Owning-flow policy | Owning dialog policy | Owning dialog policy | Owning dialog policy | Its documented parent |
| 4 | Parent screen | Parent policy | Parent policy | Parent policy | Parent policy | Its parent |
| 5 | Gameplay fallback | Open Pause | No duplicate action | Not applicable | Not applicable | Pause |

## Verification evidence

- Implementation-start commit: `git rev-parse HEAD` → `33e9567b04d06bf532ce1d7deb3b6432cb5a927a`.
- Implementation commit under test before this documentation-only finalization: `61b6e3d9658f9451f7182f12a47a7724a939e55e`.
- Final-review-fix-3 implementation commit (PR head): `490f3af177a7bb4f41db1fba4fbaf2be5cd8febd` (`fix: guard dialogue choice side effects and restore full InputMap state in tests`).
- Final matrix audit: 50 unique rows with 38 `Preserve`, 7 `Fix in HPA-376`, 2 `Fix in HPA-384`, and 3 `Replace in HPA-378/379` dispositions. HPA-569 (hosted Dialogue) converted the three NPC rows previously marked `Fix in HPA-376` (`NPC-DIALOGUE`, `NPC-SHOP`, `NPC-HEAL`) to `Preserve`; the `Fix in HPA-376` count dropped 10→7, while the 2 `Fix in HPA-384` Save/Load rows are untouched by this task.
- Implementation-start full suite: `dotnet test Sirius.sln --settings test.runsettings.local` → Passed: 863, Failed: 0, Skipped: 0, Total: 863.
- Final-review-fix-1 focused suites passed with zero failures and zero skips: `InventoryMenuControllerTest` 8; `SettingsManagerTest` 51; `SettingsDataTest` 4; `SettingsMenuControllerTest` 54; `PauseMenuDialogTest` 14; `MainMenuTest` 4; `SaveLoadDialogTest` 10; `DialogueDialogTest|HealDialogTest|PuzzleRiddleDialogTest` 6; `NpcInteractionControllerTest|ShopDialogTest` 8; `BattleManagerTest` 14; `GameInputLifecycleTest|GameTest` 55.
- Each new physical-input test also passed alone (1/1 each), and the complete per-test-isolated `GameInputLifecycleTest` suite passed 10/10 without an orphan warning.
- Final-review-fix-2 focused verification passed with zero failures and zero skips: the new stale-ownership regression 1; `SettingsManagerTest` 52; `GameInputLifecycleTest` 10; `SettingsManagerTest|GameInputLifecycleTest` 62 in one process; and `SettingsDataTest|SettingsMenuControllerTest` 58.
- Final-review-fix-3 focused verification at PR head `490f3af` passed with zero failures and zero skips: `DialogueDialogTest|HealDialogTest|PuzzleRiddleDialogTest` 7 (3 + 2 + 2). The added regression is `DialogueDialogTest.SecondTerminalChoice_AfterTerminalEmitted_GrantsOnlyFirstFlag`, covering the new `_terminalEmitted` guard in `DialogueDialog.OnChoicePressed` that prevents a second already-dispatched terminal choice from granting an additional quest flag.
- Final full suite at PR head `490f3af`: Passed: 916, Failed: 0, Skipped: 0, Total: 916 (PR #18 body evidence). The +1 delta over the final-review-fix-2 suite (915) is the single new `DialogueDialogTest` regression above.
- The +53 total relative to the 863-test implementation start is fully accounted for by these named additions:
  - Task 2 (+7): `InventoryMenuControllerTest.OpenAndClose_FromRunningTree_RestoresRunningTree`, `InventoryMenuControllerTest.OpenAndClose_FromPausedParent_RestoresPausedParent`, `InventoryMenuControllerTest.CloseMenu_CalledTwice_DoesNotOverwriteRestoredPauseState`, `InventoryMenuControllerTest.ExitTree_WhileOpen_RestoresIncomingPauseState`, `InventoryMenuControllerTest.UiCancelWhileVisible_ClosesAndRestoresIncomingPauseState`, `InventoryMenuControllerTest.ToggleInventoryWhileVisible_ClosesAndRestoresIncomingPauseState`, and `InventoryMenuControllerTest.InputWhileHidden_DoesNotChangePauseState`.
  - Task 3 (+6): `SettingsMenuControllerTest.CancelAfterStagedEdit_DoesNotMutateSnapshot_AndEmitsClosedOnce`, `SettingsMenuControllerTest.ReopenAfterCancel_AllowsOneNewClosedEmission`, `PauseMenuDialogTest.SaveButton_Pressed_HidesBeforeSignal`, `PauseMenuDialogTest.LoadButton_Pressed_HidesBeforeSignal`, `PauseMenuDialogTest.QuitToMenuButton_Pressed_HidesBeforeSignal`, and `PauseMenuDialogTest.CanceledThenCloseRequested_EmitsResumeRequestedOnce`.
  - Task 4 (+14): `MainMenuTest.QuitButton_RequestsApplicationQuitOnce`, `MainMenuTest.ShowMessage_CreatesOneVisibleAcceptDialogAndKeepsRootVisible`, `MainMenuTest.OnLoadDialogClosed_QueuesChildAndClearsReference`, `MainMenuTest.SettingsPressed_DoesNotStackAndClosedCleansOnlySettingsChild`, `SaveLoadDialogTest.Canceled_HidesAndEmitsDialogClosed`, `SaveLoadDialogTest.CanceledThenCloseRequested_EmitsOneTerminalSignal`, `SaveLoadDialogTest.MainMenuThenClose_EmitsMainMenuOnlyOnce`, `SaveLoadDialogTest.DismissOverwriteChild_LeavesParentVisibleAndEmitsNothing`, `SaveLoadDialogTest.ShowDialog_Load_HidesMainMenuButton`, `SaveLoadDialogTest.ShowDialog_SecondOpen_ResetsTerminalGuard`, `SaveLoadDialogTest.EmptySaveSlotPressed_HidesAndEmitsSaveSlotOnce`, `SaveLoadDialogTest.LoadSlotPressed_HidesAndEmitsLoadSlotOnce`, `SaveLoadDialogTest.OverwriteConfirmed_HidesAndEmitsPendingSaveSlotOnce`, and `SaveLoadDialogTest.OverwriteConfirmedThenClose_EmitsOnlySaveSlot`.
  - Task 5 (+7): `DialogueDialogTest.CanceledThenCloseRequested_EmitsDialogueClosedOnce`, `DialogueDialogTest.OutcomeThenClose_EmitsOutcomeOnly`, `DialogueDialogTest.SecondTerminalChoice_AfterTerminalEmitted_GrantsOnlyFirstFlag` (added in final-review-fix-3), `HealDialogTest.CanceledThenCloseRequested_EmitsHealCancelledOnce`, `HealDialogTest.HealThenCancel_EmitsHealCompleteOnly`, `PuzzleRiddleDialogTest.CanceledThenCloseRequested_EmitsPuzzleRiddleClosedOnce`, and `PuzzleRiddleDialogTest.ChoiceThenCancel_EmitsChoiceSelectedOnly`.
  - Task 6 (+6): `NpcInteractionControllerTest.DialogueCancel_CompletesOnceAndFreesDialogue`, `NpcInteractionControllerTest.ShopOutcome_ReplacesDialogue_AndShopCancelCompletesOnce`, `NpcInteractionControllerTest.HealOutcome_ReplacesDialogue_AndCancelCompletesOnce`, `NpcInteractionControllerTest.MissingDialogueTree_CompletesOnceAndCreatesNoDialog`, `ShopDialogTest.CanceledThenCloseRequested_EmitsShopClosedOnce`, and `ShopDialogTest.Close_CancelsPendingFeedbackTimer`.
  - Task 7 (+2 after final-review correction): `BattleManagerTest.ForceCloseDuringPreparation_EmitsOnceAndClosesImmediately` and `BattleManagerTest.ForceCloseDuringAutomaticCombat_StopsTimerClearsEffectsEmitsOnceAndClosesImmediately`. The private-seam post-result characterization was removed because public escape close immediately hides/queues and exposes no standalone escape-result dismissal.
  - Task 8 (+7): `GameInputLifecycleTest.BattleResultCancelIsHandledAndClosesResultWithoutOpeningPause`, `GameInputLifecycleTest.PauseRestorePending_ConsumesWithoutChangingVisibleStack`, `GameInputLifecycleTest.ErrorCancel_DismissesOnlyErrorAndLeavesPauseVisible`, `GameInputLifecycleTest.DefeatReturnTimerIsOwnedAndDoesNotNavigateAfterCleanup`, `GameInputLifecycleTest.DefeatReturnTimer_NavigatesOnceWhileOwnerLives`, `GameInputLifecycleTest.FloorReplacement_RebindsGridAndRefreshesPrompt`, and `GameInputLifecycleTest.InteractionPrompt_HidesDuringBattleAndRestoresAfterEscape`.
  - Final review fix 1 (+3): `GameInputLifecycleTest.ConfiguredKeyboardCancel_ClosesAcceptDialogExactlyOnce`, `GameInputLifecycleTest.ConfiguredControllerCancel_ClosesSaveLoadDialogExactlyOnce`, and `GameInputLifecycleTest.ConfiguredKeyboardCancel_ClosesTopmostRiddleAndRestoresGameplay`.
  - Final review fix 2 (+1): `SettingsManagerTest.SettingsManager_ReplacedEquivalentDialogCloseBinding_RemainsNativeAcrossResyncs`.
- Baseline and final orphan scans each contain exactly 9 matching lines: one `Detected <7> orphan nodes during test execution!`, one `Detected <10> orphan nodes during test execution!`, and seven `Detected <1> orphan nodes during test execution!` lines. No new orphan line or distinct message was introduced. The final-review-fix-3 focused run (`DialogueDialogTest|HealDialogTest|PuzzleRiddleDialogTest`) produced no orphan warning.
- HPA-569 lifecycle-doc finalization (Task 4): stale-ownership audits returned zero matches for `DialogueDialog`/`new DialogueDialog`, `choice.Condition.IsMet`/`UIInputInterception.Consumed`, and `[editable path="ModalShell"]` across runtime, test, and HPA-569 design/plan documents. The intended-symbol verification matched the shared-width, hosted Dialogue, lifecycle-test, and documentation paths, plus two pre-existing host-fixture files that use `UIScreenKinds.Dialogue` as a generic kind. Focused shared-component/Dialogue suites passed 97/97 and neighboring Shop/Heal/Pause/Settings/Prompt regressions passed 121/121; the full suite passed 1402/1402 with zero failures and zero skips; `dotnet build` succeeded with 0 errors and 0 warnings; `git diff --check` was clean. The scope audit (`git diff --name-only origin/main...HEAD`) matched the allowed runtime list plus the HPA-569 design/plan documents and the deleted `DialogueDialog` files as expected deletions; `test.runsettings.local` remains git-ignored.
- `/tmp/hpa-376-test-baseline.log` and `/tmp/hpa-376-final-review-fix-2.log` are uncommitted local evidence inputs. The completed full-suite TRX is the ignored local artifact `.superpowers/sdd/2026-07-26-sirius-ui-lifecycle-baseline/artifacts/final-review-fix-2-full.trx`; none of these evidence files is branch content. The final-review-fix-3 full-suite count (916) is reported in the PR #18 body; no separate TRX artifact was produced for that run.

## HPA-378/379 handoff

- SAVE-QUIT-TO-MAIN -> HPA-378/379: decide any Save/Load-specific confirmation while retaining hosted-child teardown, the active Pause lease, safe focus, and explicit navigation
- REWARD-TOAST -> HPA-378/379 plus downstream reward screen: non-blocking, HUD retained, cursor unchanged, no Cancel owner
- REWARD-BLOCKING -> HPA-378/379 plus HPA-393 handoff: parent inert, cursor visible, Continue focus, required acknowledgement ignores Cancel
